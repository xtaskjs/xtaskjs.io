import { Service } from "@xtaskjs/core";
import {
  InjectInternationalizationService,
  InternationalizationService,
} from "@xtaskjs/internationalization";
import {
  localizeCliCommandDocs,
  localizeCliInstallDocs,
  localizeCliOptionGroups,
  localizeDocumentationBase,
  localizeDecoratorGroups,
  localizeFlowSteps,
  localizeHighlights,
  localizePackageApiGroups,
  localizePackageDeepDive,
  localizePackageDocs,
  localizeSampleDocs,
  localizeStringList,
} from "./documentation-localization";
import { generatedPackageApiGroups } from "./generated-package-api-groups";

type DocsPage = {
  readonly href: string;
  readonly label: string;
  readonly description: string;
  readonly isCurrent: boolean;
};

type DocsHighlight = {
  readonly title: string;
  readonly text: string;
};

type DocsFlowStep = {
  readonly title: string;
  readonly text: string;
};

type PackageCapabilityScore = {
  readonly label: string;
  readonly score: number;
};

type PackageDeepDiveDoc = {
  readonly runtimeChart: readonly PackageCapabilityScore[];
  readonly lifecycle: readonly DocsFlowStep[];
  readonly usage: readonly DocsFlowStep[];
  readonly related: readonly string[];
};

type PackageNavigationLink = {
  readonly name: string;
  readonly href: string;
};

type PackageSummaryCard = {
  readonly name: string;
  readonly path: string;
  readonly tagline: string;
  readonly href: string;
};

type PackageApiGroup = {
  readonly title: string;
  readonly sourcePath: string;
  readonly exports: readonly string[];
};

type CliInstallDoc = {
  readonly title: string;
  readonly command: string;
  readonly text: string;
};

type CliCommandExample = {
  readonly title: string;
  readonly command: string;
};

type CliCommandDoc = {
  readonly name: string;
  readonly summary: string;
  readonly usage: string;
  readonly examples: readonly CliCommandExample[];
};

type CliOptionDoc = {
  readonly flag: string;
  readonly description: string;
};

type CliOptionGroupDoc = {
  readonly title: string;
  readonly description: string;
  readonly options: readonly CliOptionDoc[];
};

type PackageDoc = {
  readonly id: string;
  readonly slug: string;
  readonly name: string;
  readonly path: string;
  readonly tagline: string;
  readonly purpose: string;
  readonly install: string;
  readonly features: readonly string[];
  readonly integration: readonly string[];
  readonly sample: string;
  readonly exampleTitle: string;
  readonly exampleCode: string;
};

type SampleDoc = {
  readonly name: string;
  readonly folder: string;
  readonly stack: string;
  readonly summary: string;
  readonly endpoints: readonly string[];
  readonly flow: readonly string[];
};

type DecoratorDoc = {
  readonly id: string;
  readonly name: string;
  readonly packageName: string;
  readonly packagePath: string;
  readonly kind: string;
  readonly targets: string;
  readonly summary: string;
  readonly exampleTitle: string;
  readonly exampleCode: string;
};

type DecoratorGroupDoc = {
  readonly id: string;
  readonly title: string;
  readonly description: string;
  readonly decorators: readonly DecoratorDoc[];
};

export type DocumentationViewModel = {
  readonly title: string;
  readonly repoUrl: string;
  readonly docsPages: readonly DocsPage[];
};

export type DocumentationHubViewModel = DocumentationViewModel & {
  readonly packageCount: number;
  readonly sampleCount: number;
  readonly decoratorCount: number;
  readonly highlights: readonly DocsHighlight[];
  readonly featuredPages: readonly DocsPage[];
  readonly quickStart: {
    readonly install: string;
    readonly bootstrap: string;
  };
};

export type DocumentationArchitectureViewModel = DocumentationViewModel & {
  readonly bootstrapFlow: readonly DocsFlowStep[];
  readonly requestFlow: readonly DocsFlowStep[];
  readonly securityFlow: readonly DocsFlowStep[];
  readonly quickStart: {
    readonly install: string;
    readonly bootstrap: string;
  };
};

export type DocumentationPackagesViewModel = DocumentationViewModel & {
  readonly packageCount: number;
  readonly packages: readonly PackageDoc[];
};

export type DocumentationCliViewModel = DocumentationViewModel & {
  readonly repoUrl: string;
  readonly packageName: string;
  readonly binaryName: string;
  readonly commandCount: number;
  readonly optionGroupCount: number;
  readonly highlights: readonly DocsHighlight[];
  readonly installDocs: readonly CliInstallDoc[];
  readonly commands: readonly CliCommandDoc[];
  readonly optionGroups: readonly CliOptionGroupDoc[];
  readonly troubleshooting: readonly DocsFlowStep[];
  readonly notes: readonly string[];
};

export type DocumentationPackageDetailViewModel = DocumentationViewModel & {
  readonly packageDoc: PackageDoc & PackageDeepDiveDoc;
  readonly packageRepoUrl: string;
  readonly sampleLinks: readonly PackageNavigationLink[];
  readonly relatedPackages: readonly PackageSummaryCard[];
  readonly apiGroups: readonly PackageApiGroup[];
};

export type DocumentationSamplesViewModel = DocumentationViewModel & {
  readonly sampleCount: number;
  readonly samples: readonly SampleDoc[];
};

export type DocumentationDecoratorsViewModel = DocumentationViewModel & {
  readonly decoratorCount: number;
  readonly decoratorGroupCount: number;
  readonly packageCoverage: readonly string[];
  readonly decoratorGroups: readonly DecoratorGroupDoc[];
};

const packageDocs: readonly PackageDoc[] = [
  {
    id: "package-core",
    slug: "core",
    name: "@xtaskjs/core",
    path: "packages/core",
    tagline: "Bootstrap, kernel, DI container, lifecycle, and HTTP application primitives.",
    purpose:
      "Core owns application startup. It boots the kernel, scans components, resolves adapters, and coordinates optional integrations like TypeORM and security during CreateApplication().",
    install: "npm install @xtaskjs/core reflect-metadata",
    features: [
      "CreateApplication() bootstraps lifecycle + kernel + adapter in one entry point.",
      "Container autoloads decorated classes and resolves constructor dependencies.",
      "ApplicationLifeCycle dispatches guards, pipes, middlewares, handlers, and lifecycle events.",
      "HTTP layer normalizes adapters and view results across node, Express, and Fastify.",
    ],
    integration: [
      "Works alone with the default node-http adapter.",
      "Delegates server integration to @xtaskjs/express-http or @xtaskjs/fastify-http.",
      "Initializes @xtaskjs/typeorm and @xtaskjs/security when those packages are present.",
    ],
    sample: "01-new_app, 02-express_app, 03-fastify_app, 04-typeorm_app, 06-security_app, 07-security_express_app",
    exampleTitle: "Minimal application bootstrap",
    exampleCode: [
      'import "reflect-metadata";',
      'import { CreateApplication } from "@xtaskjs/core";',
      "",
      "async function main() {",
      "  await CreateApplication({",
      '    adapter: "node-http",',
      "    autoListen: true,",
      '    server: { host: "127.0.0.1", port: 3000 },',
      "  });",
      "}",
      "",
      "main().catch(console.error);",
    ].join("\n"),
  },
  {
    id: "package-common",
    slug: "common",
    name: "@xtaskjs/common",
    path: "packages/common",
    tagline: "Cross-package decorators, route metadata, logger, and shared execution types.",
    purpose:
      "Common provides the public decorator surface for controllers and route pipelines. It is the language the rest of the framework uses to express middleware, guard, and event metadata.",
    install: "npm install @xtaskjs/common reflect-metadata",
    features: [
      "Controller, Get, Post, Patch, Delete decorators describe routes.",
      "UseGuards, UseMiddlewares, and UsePipes compose route execution order.",
      "Logger and route metadata types are shared by framework packages.",
      "Lifecycle metadata drives runners and OnEvent listeners.",
    ],
    integration: [
      "Consumed by @xtaskjs/core during route registration and execution.",
      "Extended by @xtaskjs/security through additional decorators and guards.",
    ],
    sample: "All samples use controller decorators from @xtaskjs/common.",
    exampleTitle: "Controller metadata and pipeline composition",
    exampleCode: [
      'import { Controller, Get, Post, UseGuards } from "@xtaskjs/common";',
      "",
      '@Controller("/users")',
      '@UseGuards((context) => Boolean(context))',
      "export class UsersController {",
      '  @Get("/")',
      "  list() {",
      "    return [];",
      "  }",
      "",
      '  @Post("/")',
      "  create() {",
      "    return { created: true };",
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-value-objects",
    slug: "value-objects",
    name: "@xtaskjs/value-objects",
    path: "packages/value-objects",
    tagline: "Value object primitives, conversion helpers, DTO decorators, and DI factory integration helpers.",
    purpose:
      "Value objects gives xtaskjs projects a consistent way to model domain primitives and structured payload wrappers. It standardizes conversion from raw input, JSON strings, and serialized payloads while optionally bridging DTO transformation and DI-managed factory creation.",
    install: "npm install @xtaskjs/value-objects reflect-metadata",
    features: [
      "Base classes cover string, number, boolean, bigint, date, and JSON-backed value objects.",
      "Conversion helpers normalize raw values, JSON strings, and serialized payloads into predictable wrappers.",
      "TransformValueObject() plugs value objects into class-transformer DTO pipelines.",
      "createValueObjectFactory(), InjectableValueObjectFactory, and ValueObjectFactoryFor() support container-managed creation flows.",
    ],
    integration: [
      "Fits naturally with @xtaskjs/common ValidationPipe once DTOs are transformed through class-transformer.",
      "Can register DI-friendly factories through @xtaskjs/core when value object creation belongs in services.",
      "Useful in HTTP DTOs, domain models, and persistence boundaries where primitive normalization must stay explicit.",
    ],
    sample: "Package README examples and package tests currently provide the primary reference.",
    exampleTitle: "DTO transformation plus DI factory registration",
    exampleCode: [
      'import { plainToInstance } from "class-transformer";',
      'import {',
      '  InjectableValueObjectFactory,',
      '  StringValueObject,',
      '  TransformValueObject,',
      '  ValueObjectFactoryFor,',
      '} from "@xtaskjs/value-objects";',
      "",
      "class EmailAddress extends StringValueObject {",
      "  constructor(value: string) {",
      "    const normalized = value.trim().toLowerCase();",
      '    if (!normalized.includes("@")) {',
      '      throw new Error("Invalid email address");',
      "    }",
      "    super(normalized);",
      "  }",
      "}",
      "",
      "class CreateUserDto {",
      "  @TransformValueObject(EmailAddress)",
      "  email!: EmailAddress;",
      "}",
      "",
      "@ValueObjectFactoryFor(EmailAddress)",
      "class EmailAddressFactory extends InjectableValueObjectFactory<EmailAddress> {}",
      "",
      'const dto = plainToInstance(CreateUserDto, { email: "USER@Example.com" });',
    ].join("\n"),
  },
  {
    id: "package-express",
    slug: "express-http",
    name: "@xtaskjs/express-http",
    path: "packages/express-http",
    tagline: "Express adapter with static assets and template engine integration.",
    purpose:
      "The Express adapter translates framework routes into Express request handling. It adds static files, template engines, and JSON or rendered-view responses without changing controller code.",
    install: "npm install @xtaskjs/express-http express reflect-metadata",
    features: [
      "Wraps an existing Express application instance.",
      "Supports native Express view engines such as Handlebars.",
      "Serves static assets and rendered templates from configurable folders.",
      "Uses the same controller contracts as node-http and Fastify.",
    ],
    integration: [
      "Selected explicitly with new ExpressAdapter(expressApp).",
      "Pairs naturally with view(...) return values from controllers.",
      "Used by the current xtaskjs.io site and the 07-security_express_app sample.",
    ],
    sample: "02-express_app and 07-security_express_app",
    exampleTitle: "Express bootstrap with templates",
    exampleCode: [
      'import express from "express";',
      'import { CreateApplication } from "@xtaskjs/core";',
      'import { ExpressAdapter } from "@xtaskjs/express-http";',
      "",
      "const expressApp = express();",
      "expressApp.use(express.json());",
      "",
      "await CreateApplication({",
      "  adapter: new ExpressAdapter(expressApp),",
      "  autoListen: true,",
      '  server: { host: "127.0.0.1", port: 3000 },',
      "});",
    ].join("\n"),
  },
  {
    id: "package-fastify",
    slug: "fastify-http",
    name: "@xtaskjs/fastify-http",
    path: "packages/fastify-http",
    tagline: "Fastify adapter for the same controller model used by core and Express.",
    purpose:
      "Fastify support mirrors Express support but keeps Fastify-specific serving, view rendering, and static file behavior behind the same framework contracts.",
    install: "npm install @xtaskjs/fastify-http fastify reflect-metadata",
    features: [
      "Wraps a Fastify instance and exposes the shared xtask HTTP adapter contract.",
      "Supports static assets and file-template rendering.",
      "Preserves controller portability between Express and Fastify.",
      "Backs both pure Fastify and Fastify + TypeORM samples.",
    ],
    integration: [
      "Selected explicitly with new FastifyAdapter(fastifyApp).",
      "Good fit when you want Fastify performance but keep xtask decorators and DI.",
    ],
    sample: "03-fastify_app and 04-typeorm_app",
    exampleTitle: "Fastify bootstrap",
    exampleCode: [
      'import fastify from "fastify";',
      'import { CreateApplication } from "@xtaskjs/core";',
      'import { FastifyAdapter } from "@xtaskjs/fastify-http";',
      "",
      "const fastifyApp = fastify({ logger: true });",
      "",
      "await CreateApplication({",
      "  adapter: new FastifyAdapter(fastifyApp),",
      "  autoListen: true,",
      '  server: { host: "127.0.0.1", port: 3000 },',
      "});",
    ].join("\n"),
  },
  {
    id: "package-typeorm",
    slug: "typeorm",
    name: "@xtaskjs/typeorm",
    path: "packages/typeorm",
    tagline: "TypeORM integration, datasource registration, and repository injection helpers.",
    purpose:
      "TypeORM support attaches datasource lifecycle to xtask startup and shutdown. It gives the container a standard way to inject datasources and repositories while re-exporting the TypeORM surface.",
    install: "npm install @xtaskjs/typeorm typeorm reflect-metadata",
    features: [
      "Registers datasources during bootstrap and destroys them during app.close().",
      "Re-exports TypeORM decorators and APIs from one package entry point.",
      "Supports datasource decorators and injection helpers.",
      "Fits both SQLite demos and larger multi-datasource applications.",
    ],
    integration: [
      "Activated automatically when the package exports initializeTypeOrmIntegration().",
      "Used with Fastify in the SQLite sample and with Postgres in this website project.",
    ],
    sample: "04-typeorm_app",
    exampleTitle: "Datasource registration",
    exampleCode: [
      'import { TypeOrmDataSource } from "@xtaskjs/typeorm";',
      'import { UserEntity } from "./user.entity";',
      "",
      "@TypeOrmDataSource({",
      '  name: "default",',
      '  type: "sqlite",',
      '  database: process.env.DB_PATH || "xtask-typeorm.sqlite",',
      "  entities: [UserEntity],",
      "  synchronize: true,",
      "})",
      "export class DatabaseConfig {}",
    ].join("\n"),
  },
  {
    id: "package-cqrs",
    slug: "cqrs",
    name: "@xtaskjs/cqrs",
    path: "packages/cqrs",
    tagline: "Command, query, and event buses with handler decorators, read or write datasource aliases, and lifecycle-managed projection workflows.",
    purpose:
      "CQRS adds message buses, handler discovery, read or write repository injection, process managers, projection rebuilders, and idempotent command execution to xtaskjs. It builds on @xtaskjs/typeorm so the container can expose read and write datasources and repositories without hard-coding infrastructure details into handlers.",
    install: "npm install @xtaskjs/cqrs @xtaskjs/typeorm reflect-metadata typeorm",
    features: [
      "CommandBus, QueryBus, and EventBus are registered in the xtaskjs container and can be injected into controllers and services.",
      "CommandHandler(), QueryHandler(), EventHandler(), ProcessManager(), Saga, and ProjectionRebuilder() declare the CQRS runtime with decorators.",
      "InjectReadRepository(), InjectWriteRepository(), InjectReadDataSource(), and InjectWriteDataSource() resolve aliases backed by @xtaskjs/typeorm.",
      "IdempotentCommand() adds built-in in-memory command deduplication with an overridable idempotency store.",
    ],
    integration: [
      "Configured through configureCqrs() or @Cqrs(...) before CreateApplication() so read and write datasource names are known during bootstrap.",
      "Initializes after TypeORM registration and publishes buses, lifecycle services, datasource aliases, and repository aliases into the same DI container.",
      "Demonstrated by the 19-cqrs_app and 20-cqrs_postgres_replication_app samples, and used by this website to route admin, registration, and news flows through command and query buses.",
    ],
    sample: "19-cqrs_app and 20-cqrs_postgres_replication_app",
    exampleTitle: "Separate read and write models with injected buses",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  CommandBus,',
      '  CommandHandler,',
      '  Cqrs,',
      '  EventBus,',
      '  IdempotentCommand,',
      '  InjectCommandBus,',
      '  InjectEventBus,',
      '  InjectReadRepository,',
      '  InjectWriteRepository,',
      '  QueryBus,',
      '  QueryHandler,',
      '} from "@xtaskjs/cqrs";',
      'import { DataSource, Repository, TypeOrmDataSource } from "@xtaskjs/typeorm";',
      "",
      '@TypeOrmDataSource({ name: "write-db", type: "sqlite", database: "write.sqlite", entities: [UserEntity], synchronize: true })',
      "class WriteDatabase {}",
      "",
      '@TypeOrmDataSource({ name: "read-db", type: "sqlite", database: "read.sqlite", entities: [UserProjection], synchronize: true })',
      "class ReadDatabase {}",
      "",
      '@Cqrs({ writeDataSourceName: "write-db", readDataSourceName: "read-db" })',
      "class CqrsConfiguration {}",
      "",
      "class CreateUserCommand {",
      '  constructor(public readonly name: string, public readonly idempotencyKey: string) {}',
      "}",
      "",
      "class ListUsersQuery {}",
      "",
      "class UserCreatedEvent {",
      '  constructor(public readonly id: number, public readonly name: string) {}',
      "}",
      "",
      "@Service()",
      '@IdempotentCommand<CreateUserCommand>({ key: (command) => command.idempotencyKey })',
      "@CommandHandler(CreateUserCommand)",
      "class CreateUserHandler {",
      "  constructor(",
      "    @InjectWriteRepository(UserEntity)",
      "    private readonly writeUsers: Repository<UserEntity>,",
      "    @InjectEventBus()",
      "    private readonly events: EventBus",
      "  ) {}",
      "",
      "  async execute(command: CreateUserCommand) {",
      '    const created = await this.writeUsers.save(this.writeUsers.create({ name: command.name }));',
      "    await this.events.publish(new UserCreatedEvent(created.id, created.name));",
      "    return created.id;",
      "  }",
      "}",
      "",
      "@Service()",
      "@QueryHandler(ListUsersQuery)",
      "class ListUsersHandler {",
      "  constructor(",
      "    @InjectReadRepository(UserProjection)",
      "    private readonly readUsers: Repository<UserProjection>",
      "  ) {}",
      "",
      "  execute() {",
      '    return this.readUsers.find({ order: { id: "ASC" } });',
      "  }",
      "}",
      "",
      "@Service()",
      "class UsersFacade {",
      "  constructor(",
      "    @InjectCommandBus() private readonly commandBus: CommandBus,",
      "    @InjectQueryBus() private readonly queryBus: QueryBus",
      "  ) {}",
      "}",
    ].join("\n"),
  },
  {
    id: "package-event-source",
    slug: "event-source",
    name: "@xtaskjs/event-source",
    path: "packages/event-source",
    tagline: "Event-sourced aggregates, optimistic stream persistence, stored-event subscribers, and optional queue publication.",
    purpose:
      "Event source adds aggregate rehydration, stream persistence, optimistic concurrency checks, and stored-event publication to xtaskjs. It keeps event application inside aggregate roots while exposing repositories, stores, publishers, and subscribers through the same DI lifecycle used by the rest of the framework.",
    install: "npm install @xtaskjs/event-source reflect-metadata",
    features: [
      "EventSourcedAggregate() and ApplyEvent() let aggregates raise and replay domain events with explicit appliers.",
      "EventSourceRepository is registered in the container and supports create(), load(), loadOrCreate(), and save() with optimistic stream appends.",
      "InMemoryEventStore works by default, while createTypeOrmEventStore() adds durable event-stream persistence.",
      "createQueueEventPublisher() and EventSourceSubscriber() connect stored events to queues, projections, and side-effect handlers without replacing CQRS buses.",
    ],
    integration: [
      "Configured through @EventSource(...) or configureEventSource() before CreateApplication() so store and publisher bindings are ready during bootstrap.",
      "Pairs naturally with @xtaskjs/typeorm for durable event storage and with @xtaskjs/queues for broker-backed event publication.",
      "Works beside @xtaskjs/cqrs when commands load aggregates through repositories and downstream read models react to stored events.",
    ],
    sample: "21-event_source_rabbitmq_app and 22-event_source_cqrs_app",
    exampleTitle: "Aggregate registration with durable event storage",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  ApplyEvent,',
      '  EventSource,',
      '  EventSourcedAggregate,',
      '  EventSourcedAggregateRoot,',
      '  EventSourceRepository,',
      '  InjectEventSourceRepository,',
      '  createTypeOrmEventStore,',
      '} from "@xtaskjs/event-source";',
      '',
      '@EventSource({',
      '  store: createTypeOrmEventStore({ dataSourceName: "write-db", tableName: "event_store" }),',
      '})',
      'class EventSourceConfiguration {}',
      '',
      'class UserRegisteredEvent {',
      '  constructor(public readonly id: string, public readonly email: string) {}',
      '}',
      '',
      '@EventSourcedAggregate({ stream: "users" })',
      'class UserAggregate extends EventSourcedAggregateRoot {',
      '  public email?: string;',
      '',
      '  register(id: string, email: string) {',
      '    this.assignStreamId(id);',
      '    this.raiseEvent(new UserRegisteredEvent(id, email));',
      '  }',
      '',
      '  @ApplyEvent(UserRegisteredEvent)',
      '  onRegistered(event: UserRegisteredEvent) {',
      '    this.email = event.email;',
      '  }',
      '}',
      '',
      '@Service()',
      'export class UserRegistrationService {',
      '  constructor(',
      '    @InjectEventSourceRepository(UserAggregate)',
      '    private readonly users: EventSourceRepository<UserAggregate>',
      '  ) {}',
      '',
      '  async register(id: string, email: string) {',
      '    const user = this.users.create(id);',
      '    user.register(id, email);',
      '    await this.users.save(user);',
      '  }',
      '}',
    ].join("\n"),
  },
  {
    id: "package-security",
    slug: "security",
    name: "@xtaskjs/security",
    path: "packages/security",
    tagline: "JWT and JWE authentication, authorization decorators, and DI-aware security lifecycle.",
    purpose:
      "Security builds on the core route pipeline. It registers strategies, authenticates requests through Passport-compatible flows, and injects auth state into route execution context for guards and controllers.",
    install: "npm install @xtaskjs/security passport passport-jwt reflect-metadata",
    features: [
      "registerJwtStrategy() and registerJweStrategy() define authentication entry points.",
      "Authenticated, Auth, Roles, and AllowAnonymous decorate public or protected routes.",
      "SecurityAuthenticationService and SecurityAuthorizationService are injected through the container.",
      "Lifecycle integration is automatic when CreateApplication() sees the package.",
    ],
    integration: [
      "Depends on @xtaskjs/core and @xtaskjs/common route metadata.",
      "Used in node-http and Express security samples, and in this admin session implementation.",
    ],
    sample: "06-security_app and 07-security_express_app",
    exampleTitle: "JWT strategy plus protected controller",
    exampleCode: [
      'import { Controller, Get } from "@xtaskjs/common";',
      'import { Authenticated, Roles, registerJwtStrategy } from "@xtaskjs/security";',
      "",
      "registerJwtStrategy({",
      '  name: "default",',
      "  default: true,",
      '  secretOrKey: process.env.JWT_SECRET,',
      "});",
      "",
      '@Controller("/admin")',
      '@Authenticated()',
      "export class AdminController {",
      '  @Get("/")',
      '  @Roles("admin")',
      "  dashboard(req: any) {",
      '    return { user: req.user.sub, roles: req.auth.roles };',
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-mailer",
    slug: "mailer",
    name: "@xtaskjs/mailer",
    path: "packages/mailer",
    tagline: "Nodemailer-backed delivery, template rendering, named transports, and DI-friendly mail services.",
    purpose:
      "Mailer integrates outbound email into the same xtask lifecycle used by controllers and persistence. It registers transports at startup, exposes injectable mail services and transporters, and supports inline, EJS, or Handlebars-backed templates.",
    install: "npm install @xtaskjs/mailer nodemailer reflect-metadata",
    features: [
      "registerMailerTransport() supports SMTP, Mailtrap, JSON transport, stream transport, and custom transporter factories.",
      "MailerService can send raw mail, render templates, and deliver template-driven messages.",
      "registerMailerTemplate(), registerEjsTemplateRenderer(), and registerHandlebarsTemplateRenderer() support reusable email views.",
      "InjectMailerService(), InjectMailerTransport(), and InjectMailerLifecycleManager() connect delivery into DI-managed services.",
    ],
    integration: [
      "Initialized automatically by @xtaskjs/core when the package is installed.",
      "Used directly in the 08-email_express_app sample and in 07-security_express_app for protected profile notifications.",
      "Works well beside security when mail actions should happen after authenticated requests.",
    ],
    sample: "07-security_express_app and 08-email_express_app",
    exampleTitle: "Named transports plus template-driven delivery",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  InjectMailerService,',
      '  MailerService,',
      '  createMailtrapTransportOptions,',
      '  registerMailerTemplate,',
      '  registerMailerTransport,',
      '} from "@xtaskjs/mailer";',
      "",
      'const transport = process.env.MAIL_TRANSPORT_PROVIDER === "smtp"',
      '  ? {',
      '      host: process.env.MAIL_SMTP_HOST || "smtp.example.com",',
      '      port: Number(process.env.MAIL_SMTP_PORT || 587),',
      '      secure: process.env.MAIL_SMTP_SECURE === "true",',
      '      auth: {',
      '        user: process.env.MAIL_SMTP_USER || "user",',
      '        pass: process.env.MAIL_SMTP_PASS || "pass",',
      '      },',
      '    }',
      '  : createMailtrapTransportOptions({',
      '      username: process.env.MAILTRAP_SMTP_USER || "user",',
      '      password: process.env.MAILTRAP_SMTP_PASS || "pass",',
      '      host: process.env.MAILTRAP_SMTP_HOST || "sandbox.smtp.mailtrap.io",',
      '      port: Number(process.env.MAILTRAP_SMTP_PORT || 2525),',
      '      secure: process.env.MAILTRAP_SMTP_SECURE === "true",',
      '    });',
      "",
      "registerMailerTransport({",
      '  name: "default",',
      '  defaults: { from: "hello@xtaskjs.dev" },',
      "  transport,",
      "  verifyOnStart: false,",
      "});",
      "",
      "registerMailerTemplate({",
      '  name: "welcome-email",',
      '  subject: "Welcome {{user.name}}",',
      '  text: "Hello {{user.name}}",',
      '  html: "<h1>Hello {{user.name}}</h1>",',
      "});",
      "",
      "@Service()",
      "export class WelcomeMailerService {",
      "  constructor(",
      "    @InjectMailerService()",
      "    private readonly mailer: MailerService",
      "  ) {}",
      "",
      "  async sendWelcome(to: string) {",
      '    return this.mailer.sendTemplate("welcome-email", { user: { name: "Ada" } }, {',
      "      message: { to },",
      "    });",
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-internationalization",
    slug: "internationalization",
    name: "@xtaskjs/internationalization",
    path: "packages/internationalization",
    tagline: "Request-aware translations, locale fallback, namespace loading, and DI-friendly formatting services.",
    purpose:
      "Internationalization adds request-scoped locale resolution, translation lookup, pluralization, and formatting helpers to the xtask lifecycle. It registers locale services before controllers resolve and exposes an injectable service for controllers, views, and domain presenters.",
    install: "npm install @xtaskjs/internationalization reflect-metadata",
    features: [
      "Request-aware translations with locale fallback and exact-count pluralization.",
      "Async namespace loading for feature-specific translation bundles.",
      "Built-in number, currency, date, and datetime formatting with optional custom formatters.",
      "InjectInternationalizationService() exposes locale-aware translation and formatting inside DI-managed classes.",
    ],
    integration: [
      "Initializes automatically before container lifecycle listeners are resolved during CreateApplication().",
      "Resolves locale from query parameters and request headers, and can be extended with custom resolvers.",
      "Used by this website and by the 09-internationalization_app and 10-internationalization_express_app samples.",
    ],
    sample: "09-internationalization_app and 10-internationalization_express_app",
    exampleTitle: "Locale registration plus injected translations",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  InjectInternationalizationService,',
      '  InternationalizationService,',
      '  configureInternationalization,',
      '  registerInternationalizationLocale,',
      '} from "@xtaskjs/internationalization";',
      "",
      "configureInternationalization({",
      '  defaultLocale: "en-US",',
      '  fallbackLocale: "en-US",',
      "});",
      "",
      "registerInternationalizationLocale({",
      '  locale: "en-US",',
      '  translations: { checkout: { total: "Total: {{amount, currency}}" } },',
      "});",
      "",
      "@Service()",
      "export class CheckoutPresenter {",
      "  constructor(",
      "    @InjectInternationalizationService()",
      "    private readonly intl: InternationalizationService",
      "  ) {}",
      "",
      "  presentTotal(amount: number) {",
      '    return this.intl.t("checkout.total", { params: { amount } });',
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-scheduler",
    slug: "scheduler",
    name: "@xtaskjs/scheduler",
    path: "packages/scheduler",
    tagline: "Cron, interval, and timeout decorators with lifecycle-managed job discovery and control.",
    purpose:
      "Scheduler adds recurring and delayed jobs to xtask services. It discovers decorated methods after the DI container is ready, runs boot jobs during initialization, starts recurring work on lifecycle ready, and exposes service APIs for inspection and manual execution.",
    install: "npm install @xtaskjs/scheduler node-cron reflect-metadata",
    features: [
      "Cron(), Every(), Interval(), and Timeout() decorators declare scheduled jobs on services.",
      "Supports runOnBoot, runOnInit, named groups, retries, and per-job retry or error hooks.",
      "SchedulerService lists jobs and groups, and can start, stop, or run them manually.",
      "Tracks runtime state such as run counts, failures, and the last execution error.",
    ],
    integration: [
      "Discovered automatically during CreateApplication() after the container has registered providers.",
      "Starts recurring jobs on lifecycle ready and stops active handles during app.close().",
      "Demonstrated by the 11-scheduler_app sample with inspection endpoints and maintenance groups.",
    ],
    sample: "11-scheduler_app",
    exampleTitle: "Scheduled jobs with grouped execution",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import { Cron, InjectSchedulerService, SchedulerService, Timeout } from "@xtaskjs/scheduler";',
      "",
      "@Service()",
      "export class ReportsScheduler {",
      "  constructor(",
      "    @InjectSchedulerService()",
      "    private readonly scheduler: SchedulerService",
      "  ) {}",
      "",
      '  @Cron("0 */5 * * * *", { name: "reports.flush", group: "reports", runOnBoot: true })',
      "  flushReports() {",
      '    console.log("flush pending reports");',
      "  }",
      "",
      '  @Timeout("30s", { name: "reports.warmup" })',
      "  warmup() {",
      '    console.log("warm cache once after startup");',
      "  }",
      "",
      "  async rerunReports() {",
      '    await this.scheduler.runGroup("reports");',
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-cache",
    slug: "cache",
    name: "@xtaskjs/cache",
    path: "packages/cache",
    tagline: "Memory and Redis-backed cache models, method decorators, runtime inspection, and browser cache policies.",
    purpose:
      "Cache adds application-level data caching and client-facing HTTP cache control to xtaskjs. It registers named cache models, exposes injectable repositories and services, layers method decorators for cache read/write behavior, and can publish browser cache headers plus operational endpoints from the same package.",
    install: "npm install @xtaskjs/cache reflect-metadata",
    features: [
      "configureCache() centralizes default driver, TTL, namespace, Redis connection options, and shared HTTP cache defaults.",
      "CacheModel(), Cacheable(), CachePut(), and CacheEvict() cover model registration plus read-through, write-through, and eviction flows.",
      "CacheService, CacheRepository, and CacheAdminService are injectable for direct entry access, runtime inspection, and admin operations.",
      "BrowserCache(), CacheView(), NoStore(), NoCache(), VaryBy(), and HttpCacheService handle Cache-Control, ETag, Last-Modified, and Vary headers.",
    ],
    integration: [
      "Initialized automatically by @xtaskjs/core when the package is installed and configureCache()/registerCacheModel() have been called.",
      "Supports in-memory stores by default and Redis-backed models when the redis client package is available.",
      "Demonstrated by the 12-cache_app, 13-cache_redis_app, 14-http_cache_web_app, and 15-fastify_http_cache_web_app samples.",
    ],
    sample: "12-cache_app, 13-cache_redis_app, 14-http_cache_web_app, 15-fastify_http_cache_web_app",
    exampleTitle: "Model caching plus browser cache control",
    exampleCode: [
      'import { Controller, Get, Param } from "@xtaskjs/common";',
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  BrowserCache,',
      '  CacheModel,',
      '  CacheRepository,',
      '  Cacheable,',
      '  InjectCacheRepository,',
      '  configureCache,',
      '  createCacheManagementController,',
      '} from "@xtaskjs/cache";',
      "",
      "configureCache({",
      '  defaultDriver: "memory",',
      '  defaultTtl: "45s",',
      '  namespace: "catalog",',
      '  httpCacheDefaults: { visibility: "public", maxAge: "2m", etag: true },',
      "});",
      "",
      '@CacheModel({ name: "products", ttl: "45s" })',
      "export class ProductCacheModel {}",
      "",
      "export const CacheManagementController = createCacheManagementController({",
      '  path: "/ops/cache",',
      "});",
      "",
      "@Service()",
      "export class CatalogService {",
      "  constructor(",
      "    @InjectCacheRepository(ProductCacheModel)",
      "    private readonly products: CacheRepository<any>",
      "  ) {}",
      "",
      '  @Cacheable({ model: ProductCacheModel, key: (id: string) => id })',
      "  async getProduct(id: string) {",
      '    return { id, generatedAt: new Date().toISOString() };',
      "  }",
      "}",
      "",
      '@Controller("/products")',
      "export class CatalogController {",
      "  constructor(private readonly catalog: CatalogService) {}",
      "",
      '  @BrowserCache({ staleWhileRevalidate: "30s" })',
      '  @Get(":id")',
      '  show(@Param("id") id: string) {',
      "    return this.catalog.getProduct(id);",
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-queues",
    slug: "queues",
    name: "@xtaskjs/queues",
    path: "packages/queues",
    tagline: "Transport-agnostic queue handlers, broker helpers, publish decorators, and lifecycle-managed consumers.",
    purpose:
      "Queues brings broker-backed and in-memory message delivery into xtaskjs. It registers named transports, discovers decorated consumers from DI-managed services, exposes injectable producer APIs, and coordinates retries, dead-letter routing, and consumer start-stop behavior through the application lifecycle.",
    install: "npm install @xtaskjs/queues reflect-metadata",
    features: [
      "configureQueues() sets default transport behavior and can disable the automatic in-memory transport.",
      "registerQueueTransport(), registerInMemoryQueueTransport(), createRabbitMqTransport(), and createMqttTransport() connect brokers or local transports.",
      "QueueHandler(), QueuePattern(), PublishToQueue(), InjectQueueService(), and InjectQueueTransport() cover consumer, producer, and transport-injection flows.",
      "QueueService can publish messages, create producers, inspect consumers and groups, and start or stop queue runtime state through lifecycle-managed APIs.",
    ],
    integration: [
      "Initialized automatically by @xtaskjs/core when the package is installed and queue configuration or decorated consumers are present.",
      "Pairs with the built-in in-memory transport for local development and tests, and with RabbitMQ or MQTT helpers for broker-backed delivery.",
      "Demonstrated by the 16-queues_memory_app and 17-queues_rabbitmq_app samples.",
    ],
    sample: "16-queues_memory_app and 17-queues_rabbitmq_app",
    exampleTitle: "Named transport plus decorated queue consumers",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import {',
      '  InjectQueueService,',
      '  PublishToQueue,',
      '  QueueHandler,',
      '  QueueService,',
      '  configureQueues,',
      '  registerInMemoryQueueTransport,',
      '} from "@xtaskjs/queues";',
      "",
      "configureQueues({",
      '  defaultTransportName: "memory",',
      '  autoCreateDefaultInMemoryTransport: false,',
      "});",
      "",
      "registerInMemoryQueueTransport({",
      '  name: "memory",',
      '  kind: "in-memory",',
      "});",
      "",
      "@Service()",
      "export class OrdersQueueService {",
      "  constructor(",
      "    @InjectQueueService()",
      "    private readonly queues: QueueService",
      "  ) {}",
      "",
      "  async publishOrder(orderId: string) {",
      '    await this.queues.publish("orders.created", { orderId });',
      "  }",
      "",
      '  @PublishToQueue("orders.completed", { transportName: "memory" })',
      "  completeOrder(orderId: string) {",
      '    return { orderId, completedAt: new Date().toISOString() };',
      "  }",
      "",
      '  @QueueHandler("orders.created", { name: "orders.created", transportName: "memory" })',
      "  onOrderCreated(payload: { orderId: string }) {",
      '    console.log("received", payload.orderId);',
      "  }",
      "}",
    ].join("\n"),
  },
  {
    id: "package-socket-io",
    slug: "socket-io",
    name: "@xtaskjs/socket-io",
    path: "packages/socket-io",
    tagline: "Socket.IO gateways, event decorators, injectable realtime services, and lifecycle-managed server attachment.",
    purpose:
      "Socket.IO integrates realtime messaging into xtaskjs without leaving the framework's DI and lifecycle model. It discovers decorated gateway services, attaches to the live HTTP server during app.listen(), exposes namespaces and broadcast helpers through DI, and tears down cleanly during app.close().",
    install: "npm install @xtaskjs/socket-io socket.io reflect-metadata",
    features: [
      "SocketGateway(), OnSocketConnection(), OnSocketDisconnect(), and OnSocketEvent() declare realtime handlers on regular @Service() classes.",
      "Returned values are sent through Socket.IO acknowledgements automatically when the client provides an ack callback.",
      "InjectSocketService(), InjectSocketServer(), InjectSocketNamespace(), and InjectSocketLifecycleManager() expose the runtime through DI.",
      "SocketIoService can broadcast events, list gateways, inspect namespaces, and reuse the lifecycle-managed Socket.IO server.",
    ],
    integration: [
      "Initialized automatically by @xtaskjs/core when the package is installed and the app starts listening on an HTTP adapter.",
      "Attaches to the live server behind node-http, Express, or Fastify without changing controller or service registration patterns.",
      "Demonstrated by the 23-socket_io_express_app sample with room joins, acknowledgements, and HTTP-triggered announcements.",
    ],
    sample: "23-socket_io_express_app",
    exampleTitle: "Decorated gateway plus service broadcast",
    exampleCode: [
      'import { Service } from "@xtaskjs/core";',
      'import { InjectSocketService, OnSocketEvent, SocketGateway, SocketIoService } from "@xtaskjs/socket-io";',
      "",
      "@Service()",
      '@SocketGateway({ namespace: "/chat", group: ["realtime", "chat"] })',
      "export class ChatGateway {",
      '  @OnSocketEvent("chat.message")',
      '  onMessage(payload: { user: string; text: string }, context: { namespace: any; socket: any }) {',
      '    context.namespace.emit("chat.message", {',
      "      ...payload,",
      "      socketId: context.socket.id,",
      '      sentAt: new Date().toISOString(),',
      "    });",
      '    return { ok: true };',
      "  }",
      "}",
      "",
      "@Service()",
      "export class AnnouncementService {",
      "  constructor(",
      "    @InjectSocketService()",
      "    private readonly sockets: SocketIoService",
      "  ) {}",
      "",
      "  announce(message: string) {",
      '    this.sockets.emit("server.announcement", { message }, { namespace: "/chat", room: "lobby" });',
      "  }",
      "}",
    ].join("\n"),
  },
];

const cliHighlights: readonly DocsHighlight[] = [
  {
    title: "Project bootstrap",
    text: "The console client can scaffold a fresh XTaskJS application from the official typescript-starter and optionally install dependencies with npm, pnpm, yarn, or bun.",
  },
  {
    title: "Artifact generation",
    text: "The generate command emits controllers, services, repositories, DTOs, guards, middlewares, modules, and full resources that follow the same decorator patterns used across the upstream samples.",
  },
  {
    title: "Cache workflow",
    text: "The current upstream CLI surface does not ship a dedicated cache generator, but create plus generate module/resource provide the supported starting point for wiring @xtaskjs/cache into a new or existing app.",
  },
  {
    title: "Operational checks",
    text: "The upstream README also documents npx usage, global-install troubleshooting, and guard, DTO, and CRUD behavior so teams can standardize their scaffolding workflow.",
  },
];

const cliInstallDocs: readonly CliInstallDoc[] = [
  {
    title: "Global npm install",
    command: [
      "npm install -g @xtaskjs/cli",
      "xtask --help",
    ].join("\n"),
    text: "Install the published package globally when you want a shell-wide xtask binary for repeated project scaffolding and code generation.",
  },
  {
    title: "One-off execution with npx",
    command: "npx @xtaskjs/cli --help",
    text: "Use npx to verify the published package or run the console client without changing the current machine's global toolchain.",
  },
  {
    title: "Run from source",
    command: [
      "git clone https://github.com/xtaskjs/xtask-cli.git",
      "cd xtask-cli",
      "npm install",
      "npm run start -- --help",
    ].join("\n"),
    text: "Run the CLI from source while iterating on templates, generators, or release packaging.",
  },
  {
    title: "Create a cache-ready application",
    command: [
      "xtask create cache-demo",
      "cd cache-demo",
      "npm install @xtaskjs/cache",
    ].join("\n"),
    text: "Use the verified create workflow from the CLI repo to bootstrap an app, then add the cache package before wiring cache decorators into generated code.",
  },
];

const cliCommandDocs: readonly CliCommandDoc[] = [
  {
    name: "create",
    summary: "Bootstraps a new XTaskJS application from the official typescript-starter archive and can install dependencies immediately after scaffolding.",
    usage: "xtask create <project-name> [directory] [options]",
    examples: [
      {
        title: "Scaffold a new application",
        command: "xtask create my-api",
      },
      {
        title: "Skip install and choose a package manager",
        command: "xtask create billing-api ./services/billing --package-manager pnpm --skip-install",
      },
    ],
  },
  {
    name: "generate",
    summary: "Emits feature-oriented source files inside an existing XTaskJS app, supporting controller, service, repository, dto, guard, middleware, module, and resource scaffolds.",
    usage: "xtask generate <type> <name> [options]",
    examples: [
      {
        title: "Generate a controller",
        command: "xtask generate controller users",
      },
      {
        title: "Generate a CRUD resource",
        command: "xtask generate resource billing --path src/modules --crud",
      },
      {
        title: "Scaffold a cache-backed resource",
        command: "xtask generate resource cache-entries --path src/modules --crud --with-dto",
      },
      {
        title: "Generate a guarded module scaffold",
        command: "xtask generate module reports --path src/modules --with-guard",
      },
    ],
  },
];

const cliOptionGroups: readonly CliOptionGroupDoc[] = [
  {
    title: "Global flags",
    description: "Applies to the top-level xtask binary regardless of subcommand.",
    options: [
      {
        flag: "--help",
        description: "Print command help and exit.",
      },
      {
        flag: "--version",
        description: "Show the installed CLI version.",
      },
    ],
  },
  {
    title: "Project creation options",
    description: "Controls how the create command chooses a destination directory and installs dependencies.",
    options: [
      {
        flag: "-f, --force",
        description: "Allow scaffolding into a non-empty destination directory.",
      },
      {
        flag: "--skip-install",
        description: "Download the starter but do not run the selected package manager afterward.",
      },
      {
        flag: "--package-manager <manager>",
        description: "Choose which package manager to run after scaffolding: npm, pnpm, yarn, or bun.",
      },
    ],
  },
  {
    title: "Artifact generation options",
    description: "Shapes where generated files are written and how much scaffold code is emitted.",
    options: [
      {
        flag: "--path <directory>",
        description: "Resolve generated output relative to a different source directory.",
      },
      {
        flag: "--route <path>",
        description: "Override the route path used by generated controllers.",
      },
      {
        flag: "--flat",
        description: "Write files directly into the target path instead of creating a feature subdirectory.",
      },
      {
        flag: "--with-guard",
        description: "Generate a guard file and wire it into module or resource controllers.",
      },
      {
        flag: "--with-dto",
        description: "For resource scaffolds, also emit a DTO file for request validation.",
      },
      {
        flag: "--crud",
        description: "For resource scaffolds, emit CRUD-style controller, service, repository, and DTO code.",
      },
      {
        flag: "-f, --force",
        description: "Overwrite existing files instead of aborting when the destination already exists.",
      },
    ],
  },
];

const cliTroubleshootingFlow: readonly DocsFlowStep[] = [
  {
    title: "1. Check the active Node environment",
    text: "Confirm node -v and the active npm global prefix before assuming the xtask binary is broken.",
  },
  {
    title: "2. Inspect the global install",
    text: "Run npm list -g --depth=0 @xtaskjs/cli and type -a xtask to see whether the current shell can resolve the installed package.",
  },
  {
    title: "3. Reinstall for the active runtime",
    text: "If you use nvm or multiple Node versions, reinstall @xtaskjs/cli in the active version and refresh the shell hash.",
  },
  {
    title: "4. Verify with a direct invocation",
    text: "Use xtask --help or npx @xtaskjs/cli --help to confirm the published package works before troubleshooting project-specific commands.",
  },
];

const cliNotes: readonly string[] = [
  "The create command downloads the starter project from xtaskjs/typescript-starter.",
  "Supported generate types: controller, service, repository, resource, dto, guard, middleware, and module.",
  "Resource and module scaffolds create a feature directory by default; pass --flat to write directly into the chosen path.",
  "The --with-guard flag adds a guard file and applies @UseGuards(...) to generated module or resource controllers.",
  "The --with-dto flag only applies to resource scaffolds, and --crud upgrades the same scaffold to CRUD-style controller, service, repository, and DTO code.",
  "Generated DTOs assume class-validator is installed and may require class-transformer for richer validation pipelines.",
  "The current upstream CLI surface does not ship a dedicated cache generator; scaffold a module or resource first, then add @xtaskjs/cache configuration and decorators inside the generated files.",
  "A practical cache flow is: xtask create cache-demo, xtask generate resource cache-entries --path src/modules --crud --with-dto, then add configureCache(), CacheModel(), and cache decorators in the generated code.",
];

const sampleDocs: readonly SampleDoc[] = [
  {
    name: "01-new_app",
    folder: "samples/01-new_app",
    stack: "node-http + core",
    summary: "Smallest possible application. It proves that core can boot a server and route a health endpoint with no external adapter.",
    endpoints: ["GET /health"],
    flow: [
      "Install dependencies inside the sample folder.",
      "Run npm start to boot CreateApplication() with node-http.",
      "Call /health to verify controller registration and logger wiring.",
    ],
  },
  {
    name: "02-express_app",
    folder: "samples/02-express_app",
    stack: "core + express-http",
    summary: "Shows how an existing Express app is wrapped by ExpressAdapter while controllers still return framework-native values.",
    endpoints: ["GET /", "GET /health"],
    flow: [
      "Create an Express instance and enable body parsing or other middleware yourself.",
      "Pass the instance into new ExpressAdapter(expressApp).",
      "Render a template-backed home page and expose a JSON health endpoint.",
    ],
  },
  {
    name: "03-fastify_app",
    folder: "samples/03-fastify_app",
    stack: "core + fastify-http",
    summary: "Demonstrates adapter portability: the same controller style runs on Fastify without changing business logic.",
    endpoints: ["GET /", "GET /health"],
    flow: [
      "Create a Fastify instance with its own logger/runtime options.",
      "Wrap it with FastifyAdapter.",
      "Reuse the same controller conventions used by Express and node-http.",
    ],
  },
  {
    name: "04-typeorm_app",
    folder: "samples/04-typeorm_app",
    stack: "fastify-http + typeorm",
    summary: "Adds persistent state on top of the Fastify adapter. The sample seeds users and demonstrates datasource lifecycle registration.",
    endpoints: ["GET /users/", "POST /users/seed", "GET /health/"],
    flow: [
      "Decorate a datasource configuration class with TypeOrmDataSource().",
      "Boot the app with FastifyAdapter.",
      "Use injected repositories or datasources inside services to query and mutate state.",
    ],
  },
  {
    name: "06-security_app",
    folder: "samples/06-security_app",
    stack: "node-http + security",
    summary: "Demonstrates JWT and JWE flows without a full web framework adapter, focusing on strategy registration and protected controllers.",
    endpoints: [
      "GET /health/",
      "GET /auth/jwt/admin",
      "GET /me/",
      "GET /admin/",
      "GET /encrypted/",
    ],
    flow: [
      "Register JWT and JWE strategies before CreateApplication().",
      "Issue a demo token from /auth/jwt/admin.",
      "Use Authorization: Bearer <token> against /me/ and /admin/.",
    ],
  },
  {
    name: "07-security_express_app",
    folder: "samples/07-security_express_app",
    stack: "express-http + security + mailer",
    summary: "Combines Express integration with security decorators and the mailer module, showing authenticated profile endpoints plus protected transactional and notification emails.",
    endpoints: [
      "GET /health/",
      "GET /auth/jwt/admin",
      "GET /me/",
      "POST /me/notify",
      "GET /admin/",
      "GET /encrypted/",
    ],
    flow: [
      "Create an Express app, register JWT and JWE strategies, then register mail transports and templates at startup.",
      "Fetch a demo token from /auth/jwt/admin and call /me/ with Authorization: Bearer <token>.",
      "POST to /me/notify to render profile email templates and deliver through the default and notifications transports.",
    ],
  },
  {
    name: "08-email_express_app",
    folder: "samples/08-email_express_app",
    stack: "express-http + mailer",
    summary: "Dedicated mailer sample showing named transports, EJS-backed email templates, and DI-managed services that send welcome and campaign messages.",
    endpoints: ["GET /health/", "GET /email/", "POST /email/welcome", "POST /email/campaign"],
    flow: [
      "Register the default and notifications transports, then attach the ejs-file renderer and email templates.",
      "Start the Express app through CreateApplication() and inspect /email/ for the sample contract.",
      "POST to /email/welcome or /email/campaign to render templates and send through the configured transporter.",
    ],
  },
  {
    name: "09-internationalization_app",
    folder: "samples/09-internationalization_app",
    stack: "node-http + internationalization",
    summary: "Shows request-scoped locale resolution, translation helpers, custom formatters, and lazy namespace loading on the default node-http adapter.",
    endpoints: [
      "GET /health",
      "GET /i18n",
      "GET /i18n?locale=es-ES&name=ada&items=3&amount=1499.95",
      "GET /i18n/checkout?locale=en-US&items=2&amount=249.5",
    ],
    flow: [
      "Configure default locale, fallback locale, and built-in formatters before CreateApplication().",
      "Open /i18n to inspect translated messages, locale state, and loaded namespaces.",
      "Hit /i18n/checkout to trigger lazy namespace loading and localized number, currency, and datetime formatting.",
    ],
  },
  {
    name: "10-internationalization_express_app",
    folder: "samples/10-internationalization_express_app",
    stack: "express-http + internationalization",
    summary: "Combines localized page rendering with the Express adapter, including query-driven locale switching, Accept-Language support, and lazy checkout translations.",
    endpoints: [
      "GET /?locale=en-US&name=ada&items=3&amount=1499.95",
      "GET /?locale=es-ES&name=ada&items=3&amount=1499.95",
      "GET /checkout?locale=es-ES&name=ada&items=2&amount=249.5",
      "GET /health",
    ],
    flow: [
      "Configure locale catalogs and custom formatters, then boot an Express app through ExpressAdapter.",
      "Open the localized home page and switch between en-US and es-ES through query parameters or Accept-Language.",
      "Follow the checkout route to exercise lazy namespace loading with server-rendered views.",
    ],
  },
  {
    name: "11-scheduler_app",
    folder: "samples/11-scheduler_app",
    stack: "node-http + scheduler",
    summary: "Demonstrates scheduled jobs integrated with xtask lifecycle, including boot execution, named groups, retries, and runtime inspection endpoints.",
    endpoints: [
      "GET /health",
      "GET /scheduler/status",
      "GET /scheduler/groups",
      "GET /scheduler/run-maintenance",
    ],
    flow: [
      "Start the sample to let CreateApplication() discover scheduled methods after the container boots.",
      "Open /scheduler/status to inspect job metadata, counters, failures, and recent events.",
      "Call /scheduler/run-maintenance to trigger a named job group manually and observe retry behavior.",
    ],
  },
  {
    name: "12-cache_app",
    folder: "samples/12-cache_app",
    stack: "node-http + cache",
    summary: "Introduces cache models, method decorators, repository injection, and the built-in cache management controller on the default node-http adapter.",
    endpoints: [
      "GET /cache/products/:id",
      "GET /cache/products/:id/inspect",
      "GET /cache/products/:id/refresh",
      "GET /cache/products/:id/evict",
      "GET /ops/cache/models",
    ],
    flow: [
      "Configure the cache package with memory storage defaults and register a ProductCacheModel before CreateApplication().",
      "Call the product, refresh, and evict endpoints to observe Cacheable, CachePut, and CacheEvict behavior.",
      "Inspect /ops/cache/models and /cache/products/:id/inspect to verify repository state and generated admin metadata.",
    ],
  },
  {
    name: "13-cache_redis_app",
    folder: "samples/13-cache_redis_app",
    stack: "node-http + cache + redis",
    summary: "Switches the same caching model to Redis so the decorators and repository APIs stay the same while storage moves out of process.",
    endpoints: [
      "GET /cache/products/:id",
      "GET /cache/products/:id/inspect",
      "GET /cache/products/:id/refresh",
      "GET /cache/products/:id/evict",
      "GET /ops/cache/models",
    ],
    flow: [
      "Configure a Redis-backed cache model or client factory before boot so the lifecycle manager can open the store.",
      "Reuse the same service decorators and repository injection patterns from the memory sample.",
      "Use the inspect and admin endpoints to confirm Redis-backed entries, TTL behavior, and explicit eviction paths.",
    ],
  },
  {
    name: "14-http_cache_web_app",
    folder: "samples/14-http_cache_web_app",
    stack: "express-http + cache",
    summary: "Shows browser-facing cache policies on rendered pages and JSON endpoints, including ETag, Last-Modified, stale-while-revalidate, and preview no-store behavior.",
    endpoints: [
      "GET /",
      "GET /articles/:slug",
      "GET /api/articles/:slug",
      "GET /preview",
      "GET /ops/cache/http/routes",
    ],
    flow: [
      "Define package-wide httpCacheDefaults and then override them per route with CacheView, BrowserCache, and NoStore decorators.",
      "Request the same pages with conditional headers to observe ETag and Last-Modified handling on cached responses.",
      "Inspect /ops/cache/http/routes to verify the effective Cache-Control policy and validators resolved for each route.",
    ],
  },
  {
    name: "15-fastify_http_cache_web_app",
    folder: "samples/15-fastify_http_cache_web_app",
    stack: "fastify-http + cache",
    summary: "Ports the browser cache sample to Fastify to show that the same cache decorators and admin controller work across adapters.",
    endpoints: [
      "GET /",
      "GET /articles/:slug",
      "GET /api/articles/:slug",
      "GET /preview",
      "GET /ops/cache/http/routes",
    ],
    flow: [
      "Boot the app through FastifyAdapter while keeping the same CacheView, BrowserCache, NoStore, and management-controller setup.",
      "Compare the page and API endpoints with the Express version to confirm adapter-portable HTTP cache behavior.",
      "Use the admin routes to inspect merged cache policy, validators, and route metadata under Fastify as well.",
    ],
  },
  {
    name: "16-queues_memory_app",
    folder: "samples/16-queues_memory_app",
    stack: "node-http + queues",
    summary: "Introduces the queues package with the built-in in-memory transport, decorated consumers, pattern listeners, and publish-on-return workflows.",
    endpoints: [
      "GET /queues/",
      "GET /queues/status",
      "POST /queues/orders",
      "POST /queues/orders/:id/complete",
    ],
    flow: [
      "Configure configureQueues() with an explicit memory transport and registerInMemoryQueueTransport() before CreateApplication().",
      "POST to /queues/orders to publish a message and watch QueueHandler and QueuePattern consumers update the in-memory event log.",
      "POST to /queues/orders/:id/complete to exercise PublishToQueue() and observe chained local queue workflows.",
    ],
  },
  {
    name: "17-queues_rabbitmq_app",
    folder: "samples/17-queues_rabbitmq_app",
    stack: "node-http + queues + rabbitmq",
    summary: "Moves the same queue model to RabbitMQ, adding topic routing, competing consumers, retries, and dead-letter handling through the broker helper.",
    endpoints: [
      "GET /health",
      "GET /queues/",
      "GET /queues/status",
      "POST /queues/orders",
      "POST /queues/orders/:id/fail",
      "POST /queues/orders/:id/complete",
    ],
    flow: [
      "Configure the default transport with createRabbitMqTransport() and registerQueueTransport() before CreateApplication().",
      "Publish orders through /queues/orders and /queues/orders/:id/fail to inspect retries, audit listeners, and dead-letter routing.",
      "Open /queues/status to inspect started consumers, transport metadata, groups, retry settings, and recent events.",
    ],
  },
  {
    name: "19-cqrs_app",
    folder: "samples/19-cqrs_app",
    stack: "node-http + typeorm + cqrs",
    summary: "Shows a full CQRS flow with separate SQLite write and read databases, injected buses, idempotent commands, event-driven projections, and projection rebuild support.",
    endpoints: [
      "GET /cqrs/",
      "GET /cqrs/users",
      "GET /cqrs/debug/state",
      "POST /cqrs/users",
      "POST /cqrs/users/:id/onboard",
      "POST /cqrs/projections/rebuild",
    ],
    flow: [
      "Register two TypeORM datasources and bind them to @Cqrs(...) as write-db and read-db before CreateApplication().",
      "Use CommandBus and QueryBus from the controller to create users, inspect read models, and trigger onboarding commands.",
      "Inspect /cqrs/debug/state and POST /cqrs/projections/rebuild to verify projection handlers, rebuilders, and read-model synchronization.",
    ],
  },
  {
    name: "20-cqrs_postgres_replication_app",
    folder: "samples/20-cqrs_postgres_replication_app",
    stack: "node-http + typeorm + cqrs + postgres",
    summary: "Extends the CQRS sample to Postgres master and replica datasources so writes stay on the primary, reads come from the replica, and projection rebuild tooling remains part of the same lifecycle.",
    endpoints: [
      "GET /cqrs/",
      "GET /cqrs/users",
      "GET /cqrs/debug/state",
      "POST /cqrs/users",
      "POST /cqrs/users/:id/rename",
      "POST /cqrs/projections/rebuild",
    ],
    flow: [
      "Configure @xtaskjs/typeorm datasources for pg-master and pg-slave, then point @Cqrs(...) at those aliases before bootstrap.",
      "Route write-side commands through CommandBus while query handlers and injected read repositories use the replica datasource.",
      "Use the debug and rebuild endpoints to inspect replication-aware state, registered projection rebuilders, and lifecycle-managed read-model recovery.",
    ],
  },
  {
    name: "21-event_source_rabbitmq_app",
    folder: "samples/21-event_source_rabbitmq_app",
    stack: "node-http + event-source + typeorm + queues + rabbitmq",
    summary: "Demonstrates event-sourced aggregate writes backed by a SQLite event store, in-process projection updates, and RabbitMQ publication of stored domain events.",
    endpoints: [
      "GET /health",
      "GET /event-source/",
      "GET /event-source/users",
      "GET /event-source/streams/:id",
      "GET /event-source/status",
      "POST /event-source/users",
      "POST /event-source/users/:id/verify-email",
    ],
    flow: [
      "Configure @EventSource(...) with createTypeOrmEventStore() and createQueueEventPublisher(), then register a RabbitMQ transport before CreateApplication().",
      "Create and evolve aggregates through the POST endpoints, then inspect /event-source/streams/:id to verify replayed state and persisted event history.",
      "Open /event-source/status to inspect projection counts, started queue consumers, registered transports, and broker audit information.",
    ],
  },
  {
    name: "22-event_source_cqrs_app",
    folder: "samples/22-event_source_cqrs_app",
    stack: "node-http + event-source + typeorm + cqrs",
    summary: "Shows the package boundary where event-source owns aggregate writes and stream persistence while CQRS builds and queries a separate read model.",
    endpoints: [
      "GET /health",
      "GET /interop/",
      "GET /interop/users",
      "GET /interop/state",
      "GET /interop/streams/:id",
      "POST /interop/users",
      "POST /interop/users/:id/verify-email",
    ],
    flow: [
      "Register the event-store datasource and the separate read datasource, then bind event-source and CQRS to those aliases before bootstrap.",
      "Create users through the event-source endpoints and inspect /interop/streams/:id to confirm aggregate replay and stored event history.",
      "Query /interop/users and /interop/state to verify CQRS projections, runtime state, and the read-side model fed from persisted domain events.",
    ],
  },
  {
    name: "23-socket_io_express_app",
    folder: "samples/23-socket_io_express_app",
    stack: "express-http + socket-io",
    summary: "Express sample application using decorated Socket.IO gateways, room joins, automatic acknowledgements, and HTTP-triggered broadcasts.",
    endpoints: [
      "GET /",
      "GET /health",
      "GET /socket/",
      "GET /socket/status",
      "GET /socket/announce/:message",
    ],
    flow: [
      "Boot Express through ExpressAdapter and let @xtaskjs/socket-io attach to the live HTTP server during listen().",
      "Open the demo page, join the /chat namespace lobby, and emit chat.join plus chat.message events from the browser client.",
      "Call /socket/announce/:message to publish a server.announcement event from an HTTP controller through InjectSocketService().",
    ],
  },
];

const packageDeepDiveDocs: Readonly<Record<string, PackageDeepDiveDoc>> = {
  core: {
    runtimeChart: [
      { label: "Bootstrap", score: 5 },
      { label: "Dependency Injection", score: 5 },
      { label: "HTTP Delivery", score: 5 },
      { label: "Rendering", score: 4 },
      { label: "Integrations", score: 5 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Import reflect-metadata and decorate controllers, services, and runners so the kernel can discover them during bootstrap.",
      },
      {
        title: "During CreateApplication()",
        text: "Core builds the application lifecycle, selects the HTTP adapter, loads the DI container, and initializes optional integrations such as TypeORM, security, mailer, scheduler, and internationalization.",
      },
      {
        title: "During app.close()",
        text: "Core emits shutdown lifecycle events, closes the active adapter, and asks installed integrations to release resources in a predictable order.",
      },
    ],
    usage: [
      {
        title: "1. Install and import",
        text: "Install @xtaskjs/core with reflect-metadata and use CreateApplication() as the single bootstrap entry point.",
      },
      {
        title: "2. Pick an adapter",
        text: "Start with node-http for the smallest runtime, or pair core with Express or Fastify adapters when you need ecosystem-specific middleware or rendering.",
      },
      {
        title: "3. Layer integrations later",
        text: "Add persistence, security, mail, localization, or scheduled jobs without changing the controller and DI patterns you started with.",
      },
    ],
    related: ["common", "express-http", "fastify-http"],
  },
  common: {
    runtimeChart: [
      { label: "Bootstrap", score: 3 },
      { label: "Dependency Injection", score: 4 },
      { label: "HTTP Delivery", score: 5 },
      { label: "Security", score: 3 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Common decorators attach route, guard, middleware, pipe, and lifecycle metadata while modules are imported.",
      },
      {
        title: "During CreateApplication()",
        text: "Core reads the metadata from @xtaskjs/common to register controller routes, lifecycle listeners, and execution pipelines.",
      },
      {
        title: "During app.close()",
        text: "Common itself does not hold resources, but its lifecycle metadata continues to shape how shutdown listeners run.",
      },
    ],
    usage: [
      {
        title: "1. Define controllers",
        text: "Use Controller, Get, Post, Patch, and Delete to describe the public HTTP surface of your application.",
      },
      {
        title: "2. Compose the pipeline",
        text: "Attach UseGuards, UseMiddlewares, and UsePipes to keep authentication, cross-cutting logic, and validation close to the route.",
      },
      {
        title: "3. Add lifecycle hooks",
        text: "Use ApplicationRunner, CommandLineRunner, and OnEvent when startup work or runtime events should be expressed with the same decorator-first model.",
      },
    ],
    related: ["core", "security", "typeorm"],
  },
  "express-http": {
    runtimeChart: [
      { label: "Bootstrap", score: 3 },
      { label: "HTTP Delivery", score: 5 },
      { label: "Rendering", score: 5 },
      { label: "Integrations", score: 4 },
      { label: "Messaging", score: 3 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Create an Express app, register its middleware stack, and optionally configure template engines or static asset behavior.",
      },
      {
        title: "During CreateApplication()",
        text: "The Express adapter maps xtaskjs routes into Express handlers and preserves view(), JSON, and status-code responses.",
      },
      {
        title: "During app.close()",
        text: "The xtask application stops the active server while your existing Express middleware and configuration remain the same application-wide boundary.",
      },
    ],
    usage: [
      {
        title: "1. Create the Express app",
        text: "Enable JSON parsing, cookies, sessions, or any other Express middleware before handing control to xtaskjs.",
      },
      {
        title: "2. Pass the adapter",
        text: "Wrap the instance with new ExpressAdapter(expressApp) and pass it into CreateApplication().",
      },
      {
        title: "3. Return framework-native results",
        text: "Keep controllers portable by returning plain objects or view() results while the adapter handles Express-specific rendering details.",
      },
    ],
    related: ["core", "mailer", "internationalization"],
  },
  "fastify-http": {
    runtimeChart: [
      { label: "Bootstrap", score: 3 },
      { label: "HTTP Delivery", score: 5 },
      { label: "Rendering", score: 4 },
      { label: "Integrations", score: 4 },
      { label: "Performance", score: 5 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Create a Fastify instance with your preferred logger and plugins before wiring it into the framework adapter.",
      },
      {
        title: "During CreateApplication()",
        text: "The Fastify adapter translates the shared xtaskjs controller contract into Fastify routes, static assets, and template lookups.",
      },
      {
        title: "During app.close()",
        text: "Shutdown flows through the application close sequence so Fastify resources stop together with the rest of the xtask lifecycle.",
      },
    ],
    usage: [
      {
        title: "1. Create the Fastify app",
        text: "Start from a Fastify instance when you want lower-level performance tuning or plugin-driven composition.",
      },
      {
        title: "2. Wrap it with the adapter",
        text: "Pass new FastifyAdapter(fastifyApp) into CreateApplication() to keep the same controller surface used by node-http and Express.",
      },
      {
        title: "3. Add persistence or views",
        text: "Pair the adapter with TypeORM or file-backed views without rewriting route handlers or service classes.",
      },
    ],
    related: ["core", "typeorm", "common"],
  },
  typeorm: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Persistence", score: 5 },
      { label: "Integrations", score: 5 },
      { label: "Security", score: 2 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Register datasources with registerTypeOrmDataSource() or TypeOrmDataSource() so xtaskjs knows what to initialize.",
      },
      {
        title: "During CreateApplication()",
        text: "The integration opens datasources, publishes repository and datasource tokens into the container, and makes them injectable to services and controllers.",
      },
      {
        title: "During app.close()",
        text: "Datasource connections are destroyed automatically so database shutdown is aligned with the main application lifecycle.",
      },
    ],
    usage: [
      {
        title: "1. Register datasource definitions",
        text: "Describe your datasource once and keep entities, migrations, and connection details close to the app entry point.",
      },
      {
        title: "2. Inject repositories or datasources",
        text: "Use the xtask TypeORM decorators and tokens to inject repositories into DI-managed services instead of constructing them manually.",
      },
      {
        title: "3. Let lifecycle manage connections",
        text: "Rely on bootstrap and shutdown hooks instead of manual initialize() and destroy() calls across the codebase.",
      },
    ],
    related: ["core", "common", "security"],
  },
  cqrs: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 5 },
      { label: "Persistence", score: 5 },
      { label: "Messaging", score: 5 },
      { label: "Operations", score: 5 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Register read and write datasources with @xtaskjs/typeorm, then call configureCqrs() or decorate a configuration class with @Cqrs(...) so datasource aliases and idempotency settings are ready before handler discovery.",
      },
      {
        title: "During CreateApplication()",
        text: "The CQRS lifecycle registers command, query, and event buses; discovers decorated handlers from the container; and binds read or write datasources and repositories into the same DI runtime.",
      },
      {
        title: "During app.close()",
        text: "CQRS clears handler registries, process managers, projection rebuilders, and in-memory idempotency state while TypeORM remains responsible for tearing down the underlying datasources.",
      },
    ],
    usage: [
      {
        title: "1. Configure read and write aliases",
        text: "Start by naming the datasources you want CQRS to treat as read and write sides, even if both names point to the same TypeORM datasource in a smaller application.",
      },
      {
        title: "2. Decorate handlers and projections",
        text: "Use CommandHandler, QueryHandler, EventHandler, ProcessManager, ProjectionRebuilder, and IdempotentCommand to describe each message flow instead of manually wiring buses and registries.",
      },
      {
        title: "3. Inject buses or repositories at the edges",
        text: "Inject CommandBus and QueryBus into controllers, and inject read or write repositories into handlers so HTTP delivery, orchestration, and persistence boundaries stay explicit.",
      },
    ],
    related: ["typeorm", "core", "common"],
  },
  "event-source": {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Persistence", score: 5 },
      { label: "Messaging", score: 4 },
      { label: "Domain Modeling", score: 5 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Configure the package with @EventSource(...) or configureEventSource(), then decorate aggregates and subscribers so metadata exists before the container is scanned.",
      },
      {
        title: "During CreateApplication()",
        text: "The lifecycle manager resolves the configured event store and publisher, registers aggregate repositories and tokens in the container, and wires stored-event subscribers from DI-managed services.",
      },
      {
        title: "During app.close()",
        text: "Configured stores and publishers are destroyed automatically so in-memory, TypeORM-backed, and queue-backed event infrastructure shuts down with the application.",
      },
    ],
    usage: [
      {
        title: "1. Configure storage and publication",
        text: "Start with the default in-memory store for tests and local workflows, or plug in createTypeOrmEventStore() and createQueueEventPublisher() when streams must be durable and publish outside the process.",
      },
      {
        title: "2. Model aggregates around raised events",
        text: "Decorate aggregate roots with EventSourcedAggregate(), raise domain events from command methods, and use ApplyEvent() handlers to mutate state during live execution and historical rehydration.",
      },
      {
        title: "3. Save through repositories and react with subscribers",
        text: "Inject EventSourceRepository into services or command handlers, save aggregates to append stored events, and attach EventSourceSubscriber() listeners for projections, integrations, or side effects.",
      },
    ],
    related: ["typeorm", "cqrs", "queues"],
  },
  security: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Security", score: 5 },
      { label: "HTTP Delivery", score: 3 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Register JWT or JWE strategies and decorate protected controllers so the framework knows what authentication surface to publish.",
      },
      {
        title: "During CreateApplication()",
        text: "Security initializes authentication and authorization services, wires Passport-compatible flows, and exposes auth context to the route pipeline.",
      },
      {
        title: "During app.close()",
        text: "The security lifecycle manager tears down its state together with the rest of the application runtime.",
      },
    ],
    usage: [
      {
        title: "1. Register strategies",
        text: "Call registerJwtStrategy() or registerJweStrategy() before startup, or use their decorator forms in configuration modules.",
      },
      {
        title: "2. Protect routes declaratively",
        text: "Apply Authenticated, Roles, Auth, or AllowAnonymous to controllers and route handlers instead of embedding auth checks in business logic.",
      },
      {
        title: "3. Inject security services when needed",
        text: "For advanced flows, inject the authentication, authorization, or lifecycle services from the DI container.",
      },
    ],
    related: ["core", "common", "mailer"],
  },
  mailer: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Messaging", score: 5 },
      { label: "Rendering", score: 4 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Register transports, template renderers, and reusable templates while modules load so the mail catalog is ready before requests arrive.",
      },
      {
        title: "During CreateApplication()",
        text: "The mailer lifecycle publishes MailerService, named transporters, and optional startup verification into the DI container.",
      },
      {
        title: "During app.close()",
        text: "Transports marked for shutdown are closed automatically so SMTP or test transports do not leak resources.",
      },
    ],
    usage: [
      {
        title: "1. Register one or more transports",
        text: "Start with a default transport, then add named channels like notifications when internal and external emails should be separated.",
      },
      {
        title: "2. Define templates and renderers",
        text: "Choose inline templates or file-backed EJS or Handlebars renderers based on how much reuse or designer collaboration you need.",
      },
      {
        title: "3. Inject MailerService into services",
        text: "Send template-driven messages from DI-managed application services instead of scattering transport code across controllers.",
      },
    ],
    related: ["core", "express-http", "security"],
  },
  internationalization: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Localization", score: 5 },
      { label: "Rendering", score: 4 },
      { label: "HTTP Delivery", score: 3 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Register locales, formatters, fallback settings, and optional locale resolvers so translation rules are known before request handling starts.",
      },
      {
        title: "During CreateApplication()",
        text: "Internationalization initializes request-aware locale services before controllers and views are resolved, enabling injected translations and formatting from the first request.",
      },
      {
        title: "During app.close()",
        text: "The lifecycle manager releases runtime translation state together with the rest of the application services.",
      },
    ],
    usage: [
      {
        title: "1. Configure locales and fallbacks",
        text: "Register your locale catalogs, default locale, fallback locale, and any custom formatters during startup.",
      },
      {
        title: "2. Resolve locale from the request",
        text: "Use query parameters, Accept-Language, cookies, or custom resolvers to make the current locale part of request context.",
      },
      {
        title: "3. Inject translations into pages and services",
        text: "Call the injected service inside controllers, views, presenters, and domain helpers so all formatting stays consistent.",
      },
    ],
    related: ["core", "common", "express-http"],
  },
  scheduler: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Scheduling", score: 5 },
      { label: "Operations", score: 5 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Decorate service methods with Cron, Every, Interval, or Timeout so the scheduler can discover jobs from the DI container.",
      },
      {
        title: "During CreateApplication()",
        text: "Scheduler discovery runs after the container is ready, wiring jobs into lifecycle phases and enabling boot-time execution or grouped control.",
      },
      {
        title: "During app.close()",
        text: "Recurring timers and cron handles are stopped automatically so background work does not outlive the application process.",
      },
    ],
    usage: [
      {
        title: "1. Decorate job methods",
        text: "Choose Cron for calendar schedules, Every or Interval for repeated delays, and Timeout for one-shot post-startup work.",
      },
      {
        title: "2. Organize jobs with options",
        text: "Use names, groups, retries, timezone overrides, and runOnBoot or runOnInit to reflect operational intent in code.",
      },
      {
        title: "3. Inspect and trigger jobs at runtime",
        text: "Inject SchedulerService when operators or diagnostics endpoints need to list jobs, run one immediately, or rerun a whole group.",
      },
    ],
    related: ["core", "common", "security"],
  },
  cache: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Caching", score: 5 },
      { label: "Operations", score: 5 },
      { label: "HTTP Delivery", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Configure package defaults, register cache models, and optionally export a cache management controller so storage and HTTP policy behavior are known before requests arrive.",
      },
      {
        title: "During CreateApplication()",
        text: "The cache lifecycle manager publishes CacheService, CacheAdminService, model repositories, and HttpCacheService into the container while connecting Redis-backed stores when configured.",
      },
      {
        title: "During app.close()",
        text: "Connected stores and runtime cache state are released automatically so in-memory and Redis-backed caches shut down with the rest of the application.",
      },
    ],
    usage: [
      {
        title: "1. Configure models and defaults",
        text: "Use configureCache() and CacheModel() to define namespace, driver selection, TTL behavior, Redis connectivity, and shared HTTP cache defaults once near app startup.",
      },
      {
        title: "2. Apply method decorators and injection",
        text: "Use Cacheable, CachePut, and CacheEvict on DI-managed services, then inject CacheRepository or CacheService when you need direct entry reads, writes, or inspection.",
      },
      {
        title: "3. Expose client and operator controls",
        text: "Apply BrowserCache, CacheView, NoStore, NoCache, and VaryBy to routes, and publish createCacheManagementController() when operators need runtime cache inspection endpoints.",
      },
    ],
    related: ["core", "common", "express-http"],
  },
  queues: {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Messaging", score: 5 },
      { label: "Operations", score: 5 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Configure queue defaults, register transports, and decorate consumers while modules load so the queue runtime knows which producers and handlers to wire.",
      },
      {
        title: "During CreateApplication()",
        text: "The queues lifecycle publishes QueueService and named transport tokens into the container, discovers decorated consumers, and prepares them for lifecycle-ready startup.",
      },
      {
        title: "During app.close()",
        text: "Active consumers are stopped and connected transports are disconnected automatically so broker resources shut down with the application.",
      },
    ],
    usage: [
      {
        title: "1. Configure transports and defaults",
        text: "Start with the built-in in-memory transport for local workflows, or register RabbitMQ or MQTT helpers when messages must cross process boundaries.",
      },
      {
        title: "2. Decorate consumers and publishers",
        text: "Use QueueHandler and QueuePattern for consumers, PublishToQueue for method-result events, and InjectQueueService when services need direct publish or producer APIs.",
      },
      {
        title: "3. Inspect and control the queue runtime",
        text: "Use QueueService to list groups and consumers, create producers with defaults, and start or stop queue processing for diagnostics or operations endpoints.",
      },
    ],
    related: ["core", "common", "scheduler"],
  },
  "socket-io": {
    runtimeChart: [
      { label: "Bootstrap", score: 4 },
      { label: "Dependency Injection", score: 5 },
      { label: "HTTP Delivery", score: 4 },
      { label: "Operations", score: 5 },
      { label: "Integrations", score: 4 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Configure Socket.IO defaults when needed and decorate DI-managed services with SocketGateway plus socket handler decorators so xtaskjs can discover them.",
      },
      {
        title: "During app.listen()",
        text: "The integration resolves the live HTTP server from the selected adapter, creates the Socket.IO server, registers namespace tokens, and binds discovered gateways.",
      },
      {
        title: "During app.close()",
        text: "The Socket.IO server is closed before the HTTP adapter and container are torn down so namespaces, rooms, and subscriptions stop cleanly.",
      },
    ],
    usage: [
      {
        title: "1. Configure namespace defaults",
        text: "Call configureSocketIo() when you need a non-root default namespace or custom Socket.IO server options such as CORS and transport settings.",
      },
      {
        title: "2. Decorate gateways and events",
        text: "Use SocketGateway on a regular @Service() class, then add OnSocketConnection, OnSocketDisconnect, and OnSocketEvent handlers for each namespace workflow.",
      },
      {
        title: "3. Broadcast from the rest of the app",
        text: "Inject SocketIoService, a namespace token, or the raw server when controllers and services need to emit announcements or inspect realtime runtime state.",
      },
    ],
    related: ["core", "common", "express-http", "fastify-http"],
  },
  "value-objects": {
    runtimeChart: [
      { label: "Domain modeling", score: 5 },
      { label: "Serialization", score: 5 },
      { label: "DTO pipelines", score: 4 },
      { label: "Dependency Injection", score: 4 },
      { label: "Integrations", score: 3 },
    ],
    lifecycle: [
      {
        title: "Before startup",
        text: "Import reflect-metadata before defining value objects that rely on decorator-driven DTO or DI integrations.",
      },
      {
        title: "During CreateApplication()",
        text: "The package itself does not register a lifecycle manager, but ValueObjectFactoryFor() can register DI-ready factories through @xtaskjs/core during module loading.",
      },
      {
        title: "During app.close()",
        text: "Value objects are pure wrappers with no owned resources, so shutdown usually means letting container-managed factories disappear with the rest of the app.",
      },
    ],
    usage: [
      {
        title: "1. Define value object classes",
        text: "Extend StringValueObject, NumberValueObject, DateValueObject, or JsonValueObject and normalize invariants inside the constructor.",
      },
      {
        title: "2. Transform DTO input",
        text: "Use TransformValueObject() on DTO fields when request payloads should become domain-friendly wrappers during class-transformer conversion.",
      },
      {
        title: "3. Add factories when needed",
        text: "Create ad hoc factories with createValueObjectFactory() or register InjectableValueObjectFactory implementations when services should resolve them from the container.",
      },
    ],
    related: ["common", "core", "typeorm"],
  },
};

const decoratorGroups: readonly DecoratorGroupDoc[] = [
  {
    id: "decorators-http",
    title: "HTTP Routing And Pipelines",
    description:
      "Route declaration and request-pipeline decorators exported by @xtaskjs/common. These are the core HTTP building blocks used by controllers in every sample.",
    decorators: [
      {
        id: "decorator-common-controller",
        name: "Controller",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Defines the base route path for an HTTP controller and can attach shared middlewares, guards, and pipes.",
        exampleTitle: "Route prefix on a controller",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          "",
          '@Controller("/users")',
          "export class UsersController {",
          '  @Get("/")',
          "  list() {",
          "    return [];",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-get",
        name: "Get",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a GET route handler and optionally adds route-specific middleware, guards, or pipes.",
        exampleTitle: "GET route",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          "",
          '@Controller("/health")',
          "export class HealthController {",
          '  @Get("/")',
          "  status() {",
          "    return { ok: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-post",
        name: "Post",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a POST route handler for commands, form submissions, or resource creation.",
        exampleTitle: "POST route",
        exampleCode: [
          'import { Controller, Post } from "@xtaskjs/common";',
          "",
          '@Controller("/users")',
          "export class UsersController {",
          '  @Post("/")',
          "  create(req: any) {",
          "    return { email: req.body.email, created: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-patch",
        name: "Patch",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a PATCH route handler for partial updates.",
        exampleTitle: "PATCH route",
        exampleCode: [
          'import { Controller, Patch } from "@xtaskjs/common";',
          "",
          '@Controller("/users")',
          "export class UsersController {",
          '  @Patch("/:id")',
          "  update(req: any) {",
          "    return { id: req.params.id, updated: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-delete",
        name: "Delete",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a DELETE route handler for removals and destructive operations.",
        exampleTitle: "DELETE route",
        exampleCode: [
          'import { Controller, Delete } from "@xtaskjs/common";',
          "",
          '@Controller("/users")',
          "export class UsersController {",
          '  @Delete("/:id")',
          "  remove(req: any) {",
          "    return { id: req.params.id, removed: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-usemiddlewares",
        name: "UseMiddlewares",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Adds one or more middlewares to a controller or route so cross-cutting logic runs before the handler.",
        exampleTitle: "Class-level middleware",
        exampleCode: [
          'import { Controller, Get, UseMiddlewares } from "@xtaskjs/common";',
          "",
          "const requestLogger = async (_context: any, next: () => Promise<void>) => {",
          "  await next();",
          "};",
          "",
          '@Controller("/audit")',
          "@UseMiddlewares(requestLogger)",
          "export class AuditController {",
          '  @Get("/")',
          "  list() {",
          "    return { ok: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-useguards",
        name: "UseGuards",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Attaches guard functions to a controller or route to allow, deny, or enrich request context before execution.",
        exampleTitle: "Route-level guard",
        exampleCode: [
          'import { Controller, Get, UseGuards } from "@xtaskjs/common";',
          "",
          'const adminGuard = (context: any) => context.request?.headers?.["x-role"] === "admin";',
          "",
          '@Controller("/admin")',
          "export class AdminController {",
          '  @Get("/")',
          "  @UseGuards(adminGuard)",
          "  dashboard() {",
          "    return { secure: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-usepipes",
        name: "UsePipes",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/controller.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Applies argument transformation or validation functions before a route handler consumes input.",
        exampleTitle: "Pipe-based payload cleanup",
        exampleCode: [
          'import { Controller, Post, UsePipes } from "@xtaskjs/common";',
          "",
          'const trimEmailPipe = (value: any) => ({ ...value, email: String(value.email || "").trim() });',
          "",
          '@Controller("/accounts")',
          "export class AccountController {",
          '  @Post("/")',
          "  @UsePipes(trimEmailPipe)",
          "  create(req: any) {",
          "    return req.body;",
          "  }",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-lifecycle",
    title: "Lifecycle Runners And Events",
    description:
      "Lifecycle decorators from @xtaskjs/common used to run startup logic, CLI tasks, and event handlers inside the application lifecycle.",
    decorators: [
      {
        id: "decorator-common-onevent",
        name: "OnEvent",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/onevent.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a lifecycle event handler for a given phase and execution priority.",
        exampleTitle: "Lifecycle phase listener",
        exampleCode: [
          'import { OnEvent } from "@xtaskjs/common";',
          "",
          "export class AuditListener {",
          '  @OnEvent("AFTER_CONTROLLER_HANDLER", 10)',
          "  afterHandler() {",
          '    console.log("controller completed");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-applicationrunner",
        name: "ApplicationRunner",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/applicationrunner.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Runs a method during application startup with optional priority ordering.",
        exampleTitle: "Startup seed",
        exampleCode: [
          'import { ApplicationRunner } from "@xtaskjs/common";',
          'import { Service } from "@xtaskjs/core";',
          "",
          "@Service()",
          "export class SeedRunner {",
          "  @ApplicationRunner(100)",
          "  async seed() {",
          '    console.log("seeding application data");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-common-commandlinerunner",
        name: "CommandLineRunner",
        packageName: "@xtaskjs/common",
        packagePath: "packages/common/src/decorators/core/server/commandlinerunner.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Marks a method as a command-line lifecycle runner so it can execute in CLI-oriented flows.",
        exampleTitle: "CLI task",
        exampleCode: [
          'import { CommandLineRunner } from "@xtaskjs/common";',
          'import { Service } from "@xtaskjs/core";',
          "",
          "@Service()",
          "export class ReportsRunner {",
          "  @CommandLineRunner(0)",
          "  async generate() {",
          '    console.log("generating reports");',
          "  }",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-core",
    title: "Core DI And Components",
    description:
      "Dependency-injection and component decorators from @xtaskjs/core. These mark providers for container discovery and select named bindings for injection.",
    decorators: [
      {
        id: "decorator-core-component",
        name: "Component",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/component.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Low-level component decorator that stores DI metadata such as scope, condition, name, and primary selection.",
        exampleTitle: "Custom component metadata",
        exampleCode: [
          'import { Component } from "@xtaskjs/core";',
          "",
          '@Component({ scope: "singleton", name: "clock" })',
          "export class ClockService {}",
        ].join("\n"),
      },
      {
        id: "decorator-core-service",
        name: "Service",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/stereotypes.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Convenience stereotype for registering a class as a DI-managed service component.",
        exampleTitle: "Service stereotype",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          "",
          "@Service()",
          "export class BillingService {}",
        ].join("\n"),
      },
      {
        id: "decorator-core-controller",
        name: "Controller",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/stereotypes.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a class as a controller component in the DI container. This is separate from the HTTP route decorator exported by @xtaskjs/common.",
        exampleTitle: "DI controller stereotype",
        exampleCode: [
          'import { Controller } from "@xtaskjs/core";',
          "",
          '@Controller({ name: "admin-controller" })',
          "export class AdminComponent {}",
        ].join("\n"),
      },
      {
        id: "decorator-core-repository",
        name: "Repository",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/stereotypes.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Convenience stereotype for repository-like providers managed by the DI container.",
        exampleTitle: "Repository stereotype",
        exampleCode: [
          'import { Repository } from "@xtaskjs/core";',
          "",
          "@Repository()",
          "export class UserLookupRepository {}",
        ].join("\n"),
      },
      {
        id: "decorator-core-autowired",
        name: "AutoWired / Autowired",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/autowired.ts",
        kind: "Property decorator",
        targets: "property",
        summary: "Injects a dependency into a property. The package also exports Autowired as an alias of AutoWired.",
        exampleTitle: "Property injection",
        exampleCode: [
          'import { AutoWired } from "@xtaskjs/core";',
          'import { UserService } from "./user.service";',
          "",
          "export class AccountController {",
          '  @AutoWired({ qualifier: UserService.name })',
          "  private readonly users!: UserService;",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-core-qualifier",
        name: "Qualifier",
        packageName: "@xtaskjs/core",
        packagePath: "packages/core/src/di/qualifier.ts",
        kind: "Parameter decorator",
        targets: "constructor parameter",
        summary: "Selects a named binding for constructor-parameter injection when multiple implementations share the same type.",
        exampleTitle: "Named constructor injection",
        exampleCode: [
          'import { Qualifier, Service } from "@xtaskjs/core";',
          "",
          "@Service()",
          "export class NotificationService {",
          '  constructor(@Qualifier("mailer:notifications") private readonly transport: any) {}',
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-security",
    title: "Security And Authorization",
    description:
      "Authentication, authorization, strategy, and injector decorators exported by @xtaskjs/security. These layer on top of @xtaskjs/common route metadata.",
    decorators: [
      {
        id: "decorator-security-authenticated",
        name: "Authenticated",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Requires a successful authentication result before a controller or route executes. It can target a specific strategy or strategy list.",
        exampleTitle: "Protect a controller",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { Authenticated } from "@xtaskjs/security";',
          "",
          '@Controller("/me")',
          "@Authenticated()",
          "export class ProfileController {",
          '  @Get("/")',
          "  profile(req: any) {",
          "    return req.user;",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-auth",
        name: "Auth",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Alias of Authenticated for projects that prefer a shorter decorator name.",
        exampleTitle: "Alias for Authenticated",
        exampleCode: [
          'import { Auth } from "@xtaskjs/security";',
          "",
          '@Auth(["default", "encrypted"])',
          "export class SecureAreaController {}",
        ].join("\n"),
      },
      {
        id: "decorator-security-roles",
        name: "Roles",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Applies role-based authorization requirements to an already authenticated route.",
        exampleTitle: "Role-gated endpoint",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { Authenticated, Roles } from "@xtaskjs/security";',
          "",
          '@Controller("/admin")',
          "@Authenticated()",
          "export class AdminController {",
          '  @Get("/")',
          '  @Roles("admin")',
          "  dashboard() {",
          "    return { secure: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-allowanonymous",
        name: "AllowAnonymous",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Marks a route as publicly accessible even when the surrounding controller is authenticated by default.",
        exampleTitle: "Public health check",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { AllowAnonymous, Authenticated } from "@xtaskjs/security";',
          "",
          '@Controller("/admin")',
          "@Authenticated()",
          "export class AdminController {",
          '  @Get("/health")',
          "  @AllowAnonymous()",
          "  health() {",
          "    return { ok: true };",
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-jwtstrategy",
        name: "JwtSecurityStrategy",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of registerJwtStrategy() for registering a JWT strategy definition during module loading.",
        exampleTitle: "Decorator-based JWT strategy",
        exampleCode: [
          'import { JwtSecurityStrategy } from "@xtaskjs/security";',
          "",
          "@JwtSecurityStrategy({",
          '  name: "default",',
          "  default: true,",
          '  secretOrKey: process.env.JWT_SECRET,',
          "})",
          "export class DefaultJwtStrategy {}",
        ].join("\n"),
      },
      {
        id: "decorator-security-jwestrategy",
        name: "JweSecurityStrategy",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of registerJweStrategy() for encrypted token flows.",
        exampleTitle: "Decorator-based JWE strategy",
        exampleCode: [
          'import { JweSecurityStrategy } from "@xtaskjs/security";',
          "",
          "@JweSecurityStrategy({",
          '  name: "encrypted",',
          '  decryptionKey: process.env.JWE_SECRET || "secret",',
          "})",
          "export class EncryptedStrategy {}",
        ].join("\n"),
      },
      {
        id: "decorator-security-injectauthservice",
        name: "InjectAuthenticationService",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/lifecycle.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the SecurityAuthenticationService registered by the security lifecycle manager.",
        exampleTitle: "Inject auth service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectAuthenticationService, SecurityAuthenticationService } from "@xtaskjs/security";',
          "",
          "@Service()",
          "export class SessionAuditService {",
          "  constructor(",
          "    @InjectAuthenticationService()",
          "    private readonly authentication: SecurityAuthenticationService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-injectauthorizationservice",
        name: "InjectAuthorizationService",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/lifecycle.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the SecurityAuthorizationService used for role and permission decisions.",
        exampleTitle: "Inject authorization service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectAuthorizationService, SecurityAuthorizationService } from "@xtaskjs/security";',
          "",
          "@Service()",
          "export class PolicyService {",
          "  constructor(",
          "    @InjectAuthorizationService()",
          "    private readonly authorization: SecurityAuthorizationService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-injectpassport",
        name: "InjectPassport",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/lifecycle.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the configured Passport instance managed by xtaskjs security.",
        exampleTitle: "Inject Passport",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectPassport } from "@xtaskjs/security";',
          "",
          "@Service()",
          "export class PassportInspector {",
          "  constructor(@InjectPassport() private readonly passport: any) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-security-injectlifecycle",
        name: "InjectSecurityLifecycleManager",
        packageName: "@xtaskjs/security",
        packagePath: "packages/security/src/lifecycle.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the SecurityLifecycleManager so advanced services can inspect strategies or authentication state wiring.",
        exampleTitle: "Inject lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSecurityLifecycleManager } from "@xtaskjs/security";',
          "",
          "@Service()",
          "export class SecurityDiagnosticsService {",
          "  constructor(@InjectSecurityLifecycleManager() private readonly lifecycle: any) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-typeorm",
    title: "Persistence And Repositories",
    description:
      "TypeORM registration and injection decorators exported by @xtaskjs/typeorm. These bind datasources and repositories into the same DI container.",
    decorators: [
      {
        id: "decorator-typeorm-datasource",
        name: "TypeOrmDataSource",
        packageName: "@xtaskjs/typeorm",
        packagePath: "packages/typeorm/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a TypeORM datasource definition for xtask startup and shutdown management.",
        exampleTitle: "Register a datasource",
        exampleCode: [
          'import { TypeOrmDataSource } from "@xtaskjs/typeorm";',
          'import { UserEntity } from "./user.entity";',
          "",
          "@TypeOrmDataSource({",
          '  name: "default",',
          '  type: "sqlite",',
          '  database: "app.sqlite",',
          "  entities: [UserEntity],",
          "  synchronize: true,",
          "})",
          "export class DatabaseConfig {}",
        ].join("\n"),
      },
      {
        id: "decorator-typeorm-injectdatasource",
        name: "InjectDataSource",
        packageName: "@xtaskjs/typeorm",
        packagePath: "packages/typeorm/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a named datasource instance managed by xtaskjs TypeORM integration.",
        exampleTitle: "Inject the default datasource",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { DataSource, InjectDataSource } from "@xtaskjs/typeorm";',
          "",
          "@Service()",
          "export class HealthQueryService {",
          "  constructor(@InjectDataSource() private readonly dataSource: DataSource) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-typeorm-injectrepository",
        name: "InjectRepository",
        packageName: "@xtaskjs/typeorm",
        packagePath: "packages/typeorm/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a TypeORM repository for a given entity and datasource name.",
        exampleTitle: "Inject entity repository",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectRepository, Repository } from "@xtaskjs/typeorm";',
          'import { UserEntity } from "./user.entity";',
          "",
          "@Service()",
          "export class UsersService {",
          "  constructor(",
          "    @InjectRepository(UserEntity)",
          "    private readonly users: Repository<UserEntity>",
          "  ) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-cqrs",
    title: "CQRS Buses, Handlers, And Projections",
    description:
      "CQRS configuration, handler, projection, idempotency, and injector decorators exported by @xtaskjs/cqrs. These connect command, query, and event flows to the xtaskjs container and read or write datasource aliases.",
    decorators: [
      {
        id: "decorator-cqrs-configuration",
        name: "Cqrs",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of configureCqrs() for binding the read and write datasource aliases and optional idempotency store during module loading.",
        exampleTitle: "Bind read and write datasource aliases",
        exampleCode: [
          'import { Cqrs } from "@xtaskjs/cqrs";',
          "",
          '@Cqrs({ writeDataSourceName: "write-db", readDataSourceName: "read-db" })',
          "export class CqrsConfiguration {}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-commandhandler",
        name: "CommandHandler",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a DI-managed class as the single handler for a command message type.",
        exampleTitle: "Command handler",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CommandHandler, ICommandHandler } from "@xtaskjs/cqrs";',
          "",
          "class CreateUserCommand {",
          '  constructor(public readonly name: string) {}',
          "}",
          "",
          "@Service()",
          "@CommandHandler(CreateUserCommand)",
          "export class CreateUserHandler implements ICommandHandler<CreateUserCommand, string> {",
          "  execute(command: CreateUserCommand) {",
          '    return command.name;',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-queryhandler",
        name: "QueryHandler",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a DI-managed class as the handler for a query message type.",
        exampleTitle: "Query handler",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { IQueryHandler, QueryHandler } from "@xtaskjs/cqrs";',
          "",
          "class ListUsersQuery {}",
          "",
          "@Service()",
          "@QueryHandler(ListUsersQuery)",
          "export class ListUsersHandler implements IQueryHandler<ListUsersQuery, string[]> {",
          "  execute() {",
          '    return ["Ada"];',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-eventhandler",
        name: "EventHandler",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers one or more event handlers that react after a message publishes an event on the CQRS event bus.",
        exampleTitle: "Event handler",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { EventHandler, IEventHandler } from "@xtaskjs/cqrs";',
          "",
          "class UserCreatedEvent {",
          '  constructor(public readonly id: string) {}',
          "}",
          "",
          "@Service()",
          "@EventHandler(UserCreatedEvent)",
          "export class UserProjectionHandler implements IEventHandler<UserCreatedEvent> {",
          "  async handle(event: UserCreatedEvent) {",
          '    console.log(event.id);',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-processmanager",
        name: "ProcessManager / Saga",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers an orchestration component that reacts to events with access to command, query, and event buses. Saga is an alias of ProcessManager.",
        exampleTitle: "Event-driven orchestration",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { IProcessManager, ProcessManager, ProcessManagerContext } from "@xtaskjs/cqrs";',
          "",
          "class UserCreatedEvent {",
          '  constructor(public readonly id: string) {}',
          "}",
          "",
          "@Service()",
          "@ProcessManager(UserCreatedEvent)",
          "export class WelcomeProcessManager implements IProcessManager<UserCreatedEvent> {",
          "  async handle(event: UserCreatedEvent, context: ProcessManagerContext) {",
          '    await context.commandBus.execute({ userId: event.id });',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-projectionrebuilder",
        name: "ProjectionRebuilder",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a named projection rebuilder so operators or diagnostics flows can rebuild a read model from write-side state.",
        exampleTitle: "Named projection rebuilder",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { IProjectionRebuilder, ProjectionRebuilder } from "@xtaskjs/cqrs";',
          "",
          "@Service()",
          '@ProjectionRebuilder("users")',
          "export class UserProjectionRebuilder implements IProjectionRebuilder {",
          "  async rebuild() {",
          '    console.log("rebuild read model");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-idempotentcommand",
        name: "IdempotentCommand",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Adds idempotency metadata to a command handler so repeated executions can reuse a cached result instead of running the write-side action again.",
        exampleTitle: "Idempotent command handler",
        exampleCode: [
          'import { CommandHandler, IdempotentCommand } from "@xtaskjs/cqrs";',
          "",
          "class CreateUserCommand {",
          '  constructor(public readonly name: string, public readonly idempotencyKey: string) {}',
          "}",
          "",
          '@IdempotentCommand<CreateUserCommand>({ key: (command) => command.idempotencyKey })',
          "@CommandHandler(CreateUserCommand)",
          "export class CreateUserHandler {",
          "  execute(command: CreateUserCommand) {",
          '    return `${command.name}:${command.idempotencyKey}`;',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectcommandbus",
        name: "InjectCommandBus",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects CommandBus so controllers or services can dispatch commands without reaching into the lifecycle manager directly.",
        exampleTitle: "Inject command bus",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CommandBus, InjectCommandBus } from "@xtaskjs/cqrs";',
          "",
          "@Service()",
          "export class UsersFacade {",
          "  constructor(@InjectCommandBus() private readonly commandBus: CommandBus) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectquerybus",
        name: "InjectQueryBus",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects QueryBus so read-side requests can be dispatched from controllers, presenters, or services.",
        exampleTitle: "Inject query bus",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectQueryBus, QueryBus } from "@xtaskjs/cqrs";',
          "",
          "@Service()",
          "export class ReportsFacade {",
          "  constructor(@InjectQueryBus() private readonly queryBus: QueryBus) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injecteventbus",
        name: "InjectEventBus",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects EventBus so handlers can publish follow-up domain or integration events after write-side work completes.",
        exampleTitle: "Inject event bus",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { EventBus, InjectEventBus } from "@xtaskjs/cqrs";',
          "",
          "@Service()",
          "export class UserEventsPublisher {",
          "  constructor(@InjectEventBus() private readonly events: EventBus) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectreaddatasource",
        name: "InjectReadDataSource",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the configured read-side datasource alias managed by CQRS and backed by @xtaskjs/typeorm.",
        exampleTitle: "Inject read datasource",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectReadDataSource } from "@xtaskjs/cqrs";',
          'import { DataSource } from "@xtaskjs/typeorm";',
          "",
          "@Service()",
          "export class ReadDiagnosticsService {",
          "  constructor(@InjectReadDataSource() private readonly readDataSource: DataSource) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectwritedatasource",
        name: "InjectWriteDataSource",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the configured write-side datasource alias for command handlers or projection rebuilders that need direct datasource access.",
        exampleTitle: "Inject write datasource",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectWriteDataSource } from "@xtaskjs/cqrs";',
          'import { DataSource } from "@xtaskjs/typeorm";',
          "",
          "@Service()",
          "export class WriteDiagnosticsService {",
          "  constructor(@InjectWriteDataSource() private readonly writeDataSource: DataSource) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectreadrepository",
        name: "InjectReadRepository",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a repository from the configured read-side datasource alias so queries can stay projection-focused.",
        exampleTitle: "Inject read repository",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectReadRepository } from "@xtaskjs/cqrs";',
          'import { Repository } from "@xtaskjs/typeorm";',
          "",
          "class UserProjection {}",
          "",
          "@Service()",
          "export class UsersReadService {",
          "  constructor(@InjectReadRepository(UserProjection) private readonly users: Repository<UserProjection>) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectwriterepository",
        name: "InjectWriteRepository",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a repository from the configured write-side datasource alias for command handlers and write-model maintenance flows.",
        exampleTitle: "Inject write repository",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectWriteRepository } from "@xtaskjs/cqrs";',
          'import { Repository } from "@xtaskjs/typeorm";',
          "",
          "class UserEntity {}",
          "",
          "@Service()",
          "export class UsersWriteService {",
          "  constructor(@InjectWriteRepository(UserEntity) private readonly users: Repository<UserEntity>) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cqrs-injectlifecycle",
        name: "InjectCqrsLifecycleManager",
        packageName: "@xtaskjs/cqrs",
        packagePath: "packages/cqrs/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the CqrsLifecycleManager for advanced diagnostics, projection rebuild operations, and direct runtime inspection.",
        exampleTitle: "Inject CQRS lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectCqrsLifecycleManager } from "@xtaskjs/cqrs";',
          "",
          "@Service()",
          "export class CqrsDiagnosticsService {",
          "  constructor(@InjectCqrsLifecycleManager() private readonly lifecycle: any) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-event-source",
    title: "Event Sourcing And Stored Events",
    description:
      "Configuration, aggregate, subscriber, and injector decorators exported by @xtaskjs/event-source. These wire aggregate metadata, event appliers, repositories, and stored-event subscribers into the xtaskjs lifecycle.",
    decorators: [
      {
        id: "decorator-event-source-configuration",
        name: "EventSource",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of configureEventSource() for registering the event store, publisher, and optional runtime flags during module loading.",
        exampleTitle: "Configure durable event storage",
        exampleCode: [
          'import { EventSource, createTypeOrmEventStore } from "@xtaskjs/event-source";',
          '',
          '@EventSource({',
          '  store: createTypeOrmEventStore({ dataSourceName: "write-db", tableName: "event_store" }),',
          '})',
          'export class EventSourceConfiguration {}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-aggregate",
        name: "EventSourcedAggregate",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers aggregate metadata such as the aggregate name and stream key so repositories can load and persist event streams consistently.",
        exampleTitle: "Declare an aggregate stream",
        exampleCode: [
          'import { EventSourcedAggregate, EventSourcedAggregateRoot } from "@xtaskjs/event-source";',
          '',
          '@EventSourcedAggregate({ stream: "users" })',
          'export class UserAggregate extends EventSourcedAggregateRoot {}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-applyevent",
        name: "ApplyEvent",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers the method that mutates aggregate state for a given event type during both live execution and historical replay.",
        exampleTitle: "Apply an aggregate event",
        exampleCode: [
          'import { ApplyEvent } from "@xtaskjs/event-source";',
          '',
          'class UserRegisteredEvent {',
          '  constructor(public readonly email: string) {}',
          '}',
          '',
          'export class UserAggregate {',
          '  public email?: string;',
          '',
          '  @ApplyEvent(UserRegisteredEvent)',
          '  onRegistered(event: UserRegisteredEvent) {',
          '    this.email = event.email;',
          '  }',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-subscriber",
        name: "EventSourceSubscriber / StoredEventSubscriber",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a DI-managed subscriber that reacts after stored events are appended, making it suitable for projections, integrations, and side effects.",
        exampleTitle: "React to stored events",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { EventSourceSubscriber, IEventSourceSubscriber } from "@xtaskjs/event-source";',
          '',
          'class UserRegisteredEvent {',
          '  constructor(public readonly email: string) {}',
          '}',
          '',
          '@Service()',
          '@EventSourceSubscriber(UserRegisteredEvent)',
          'export class WelcomeProjection implements IEventSourceSubscriber<UserRegisteredEvent> {',
          '  handle(event: UserRegisteredEvent) {',
          '    console.log(event.email);',
          '  }',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-injectrepository",
        name: "InjectEventSourceRepository",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the repository for a specific event-sourced aggregate so services can create, load, and save streams through the lifecycle-managed store.",
        exampleTitle: "Inject an aggregate repository",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { EventSourceRepository, InjectEventSourceRepository } from "@xtaskjs/event-source";',
          '',
          'class UserAggregate {}',
          '',
          '@Service()',
          'export class UserService {',
          '  constructor(',
          '    @InjectEventSourceRepository(UserAggregate)',
          '    private readonly users: EventSourceRepository<any>',
          '  ) {}',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-injectstore",
        name: "InjectEventStore",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the active event store implementation when advanced services need direct stream append or load access.",
        exampleTitle: "Inject the event store",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectEventStore } from "@xtaskjs/event-source";',
          '',
          '@Service()',
          'export class EventStoreDiagnosticsService {',
          '  constructor(@InjectEventStore() private readonly store: any) {}',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-injectbus",
        name: "InjectEventSourceBus",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the in-process stored-event bus so services can publish persisted envelopes to local subscribers when they need low-level runtime control.",
        exampleTitle: "Inject the stored-event bus",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectEventSourceBus } from "@xtaskjs/event-source";',
          '',
          '@Service()',
          'export class EventReplayService {',
          '  constructor(@InjectEventSourceBus() private readonly bus: any) {}',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-injectpublisher",
        name: "InjectEventPublisher",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the configured external event publisher so services can inspect or extend publication behavior beyond the default queue bridge.",
        exampleTitle: "Inject the external publisher",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectEventPublisher } from "@xtaskjs/event-source";',
          '',
          '@Service()',
          'export class EventPublisherDiagnosticsService {',
          '  constructor(@InjectEventPublisher() private readonly publisher: any) {}',
          '}',
        ].join("\n"),
      },
      {
        id: "decorator-event-source-injectlifecycle",
        name: "InjectEventSourceLifecycleManager",
        packageName: "@xtaskjs/event-source",
        packagePath: "packages/event-source/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the EventSourceLifecycleManager for repository lookup, initialization checks, subscriber inspection, and lower-level event-source runtime access.",
        exampleTitle: "Inject the event-source lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectEventSourceLifecycleManager } from "@xtaskjs/event-source";',
          '',
          '@Service()',
          'export class EventSourceDiagnosticsService {',
          '  constructor(@InjectEventSourceLifecycleManager() private readonly lifecycle: any) {}',
          '}',
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-mailer",
    title: "Mailer Templates And Delivery",
    description:
      "Mailer decorators exported by @xtaskjs/mailer. These register transports and templates and inject delivery services into DI-managed classes.",
    decorators: [
      {
        id: "decorator-mailer-transport",
        name: "MailerTransport",
        packageName: "@xtaskjs/mailer",
        packagePath: "packages/mailer/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of registerMailerTransport() for registering a named mail transport during module loading.",
        exampleTitle: "Register a transport with a decorator",
        exampleCode: [
          'import { MailerTransport, createMailtrapTransportOptions } from "@xtaskjs/mailer";',
          "",
          'const transport = process.env.MAIL_TRANSPORT_PROVIDER === "smtp"',
          '  ? {',
          '      host: process.env.MAIL_SMTP_HOST || "smtp.example.com",',
          '      port: Number(process.env.MAIL_SMTP_PORT || 587),',
          '      secure: process.env.MAIL_SMTP_SECURE === "true",',
          '      auth: {',
          '        user: process.env.MAIL_SMTP_USER || "user",',
          '        pass: process.env.MAIL_SMTP_PASS || "pass",',
          '      },',
          '    }',
          '  : createMailtrapTransportOptions({',
          '      username: process.env.MAILTRAP_SMTP_USER || "user",',
          '      password: process.env.MAILTRAP_SMTP_PASS || "pass",',
          '      host: process.env.MAILTRAP_SMTP_HOST || "sandbox.smtp.mailtrap.io",',
          '      port: Number(process.env.MAILTRAP_SMTP_PORT || 2525),',
          '      secure: process.env.MAILTRAP_SMTP_SECURE === "true",',
          '    });',
          "",
          "@MailerTransport({",
          '  name: "default",',
          '  defaults: { from: "hello@xtaskjs.dev" },',
          "  transport,",
          "})",
          "export class DefaultTransportRegistration {}",
        ].join("\n"),
      },
      {
        id: "decorator-mailer-template",
        name: "MailerTemplate",
        packageName: "@xtaskjs/mailer",
        packagePath: "packages/mailer/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of registerMailerTemplate() for reusable inline or file-rendered email templates.",
        exampleTitle: "Register a template",
        exampleCode: [
          'import { MailerTemplate } from "@xtaskjs/mailer";',
          "",
          "@MailerTemplate({",
          '  name: "welcome",',
          '  subject: "Welcome {{user.name}}",',
          '  text: "Hello {{user.name}}",',
          '  html: "<h1>Hello {{user.name}}</h1>",',
          "})",
          "export class WelcomeTemplateRegistration {}",
        ].join("\n"),
      },
      {
        id: "decorator-mailer-injectservice",
        name: "InjectMailerService",
        packageName: "@xtaskjs/mailer",
        packagePath: "packages/mailer/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects MailerService so a DI-managed service can render templates and send mail.",
        exampleTitle: "Inject mailer service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectMailerService, MailerService } from "@xtaskjs/mailer";',
          "",
          "@Service()",
          "export class EmailService {",
          "  constructor(@InjectMailerService() private readonly mailer: MailerService) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-mailer-injecttransport",
        name: "InjectMailerTransport",
        packageName: "@xtaskjs/mailer",
        packagePath: "packages/mailer/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a named transport so a service can send directly on a specific channel such as notifications.",
        exampleTitle: "Inject notifications transport",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectMailerTransport, MailerTransporter } from "@xtaskjs/mailer";',
          "",
          "@Service()",
          "export class AlertsService {",
          "  constructor(",
          '    @InjectMailerTransport("notifications")',
          "    private readonly notifications: MailerTransporter",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-mailer-injectlifecycle",
        name: "InjectMailerLifecycleManager",
        packageName: "@xtaskjs/mailer",
        packagePath: "packages/mailer/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the MailerLifecycleManager for advanced inspection, verification, or transporter lookup.",
        exampleTitle: "Inject lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectMailerLifecycleManager } from "@xtaskjs/mailer";',
          "",
          "@Service()",
          "export class MailDiagnosticsService {",
          "  constructor(@InjectMailerLifecycleManager() private readonly lifecycle: any) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-internationalization",
    title: "Internationalization And Locale Resolution",
    description:
      "Configuration and injector decorators exported by @xtaskjs/internationalization. These register locale behavior and expose translation services inside DI-managed classes.",
    decorators: [
      {
        id: "decorator-internationalization-configuration",
        name: "Internationalization",
        packageName: "@xtaskjs/internationalization",
        packagePath: "packages/internationalization/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of configureInternationalization() for registering default locale, fallback locale, currency, and timezone settings during module loading.",
        exampleTitle: "Register base internationalization settings",
        exampleCode: [
          'import { Internationalization } from "@xtaskjs/internationalization";',
          "",
          "@Internationalization({",
          '  defaultLocale: "en-US",',
          '  fallbackLocale: "en-US",',
          '  defaultCurrency: "USD",',
          '  defaultTimeZone: "UTC",',
          "})",
          "export class AppI18nConfiguration {}",
        ].join("\n"),
      },
      {
        id: "decorator-internationalization-locale",
        name: "InternationalizationLocale",
        packageName: "@xtaskjs/internationalization",
        packagePath: "packages/internationalization/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a locale definition with translations, locale-specific currency and timezone values, and optional namespace dictionaries.",
        exampleTitle: "Register a locale catalog",
        exampleCode: [
          'import { InternationalizationLocale } from "@xtaskjs/internationalization";',
          "",
          "@InternationalizationLocale({",
          '  locale: "es-ES",',
          '  currency: "EUR",',
          '  timeZone: "Europe/Madrid",',
          '  translations: { home: { title: "Bienvenida" } },',
          "})",
          "export class SpanishLocaleRegistration {}",
        ].join("\n"),
      },
      {
        id: "decorator-internationalization-resolver",
        name: "InternationalizationResolver",
        packageName: "@xtaskjs/internationalization",
        packagePath: "packages/internationalization/src/configuration.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a custom locale resolver that can derive locale context from the request, headers, container state, or tenant metadata.",
        exampleTitle: "Custom locale resolver",
        exampleCode: [
          'import { InternationalizationResolver } from "@xtaskjs/internationalization";',
          "",
          "@InternationalizationResolver(({ request }) => {",
          '  return request?.headers?.["x-locale"] || request?.query?.locale;',
          "})",
          "export class HeaderLocaleResolver {}",
        ].join("\n"),
      },
      {
        id: "decorator-internationalization-injectservice",
        name: "InjectInternationalizationService",
        packageName: "@xtaskjs/internationalization",
        packagePath: "packages/internationalization/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects InternationalizationService so controllers and services can translate keys, format values, inspect locales, and load namespaces on demand.",
        exampleTitle: "Inject translation service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectInternationalizationService, InternationalizationService } from "@xtaskjs/internationalization";',
          "",
          "@Service()",
          "export class CheckoutPresenter {",
          "  constructor(",
          "    @InjectInternationalizationService()",
          "    private readonly intl: InternationalizationService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-internationalization-injectlifecycle",
        name: "InjectInternationalizationLifecycleManager",
        packageName: "@xtaskjs/internationalization",
        packagePath: "packages/internationalization/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the InternationalizationLifecycleManager for advanced inspection of loaded locales, namespaces, request context, or formatter registration.",
        exampleTitle: "Inject internationalization lifecycle",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectInternationalizationLifecycleManager } from "@xtaskjs/internationalization";',
          "",
          "@Service()",
          "export class LocaleDiagnosticsService {",
          "  constructor(",
          "    @InjectInternationalizationLifecycleManager()",
          "    private readonly lifecycle: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-value-objects",
    title: "Value Objects And DTO Transformation",
    description:
      "DTO-oriented decorators from @xtaskjs/value-objects that turn raw request fields into normalized domain wrappers before controllers and services consume them.",
    decorators: [
      {
        id: "decorator-value-objects-transform",
        name: "TransformValueObject",
        packageName: "@xtaskjs/value-objects",
        packagePath: "packages/value-objects/src/decorators.ts",
        kind: "Property decorator",
        targets: "class property",
        summary: "Transforms raw DTO input into a value object instance during class-transformer conversion so downstream code receives a validated wrapper instead of a primitive.",
        exampleTitle: "Normalize DTO email input",
        exampleCode: [
          'import { plainToInstance } from "class-transformer";',
          'import { StringValueObject, TransformValueObject } from "@xtaskjs/value-objects";',
          "",
          "class EmailAddress extends StringValueObject {",
          "  constructor(value: string) {",
          "    super(value.trim().toLowerCase());",
          "  }",
          "}",
          "",
          "class CreateUserDto {",
          "  @TransformValueObject(EmailAddress)",
          "  email!: EmailAddress;",
          "}",
          "",
          'const dto = plainToInstance(CreateUserDto, { email: " USER@Example.com " });',
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-cache-runtime",
    title: "Cache Models And Runtime Control",
    description:
      "Configuration, model, injector, and method decorators from @xtaskjs/cache used to register cache models, inject repositories or services, and control read/write cache behavior in DI-managed services.",
    decorators: [
      {
        id: "decorator-cache-settings",
        name: "CacheSettings",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Decorator form of configureCache() for setting package-wide defaults such as driver, TTL, namespace, Redis options, and HTTP cache defaults while modules load.",
        exampleTitle: "Decorator-based cache defaults",
        exampleCode: [
          'import { CacheSettings } from "@xtaskjs/cache";',
          "",
          "@CacheSettings({",
          '  defaultDriver: "memory",',
          '  defaultTtl: "30s",',
          '  namespace: "catalog",',
          "})",
          "export class CacheConfiguration {}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-model",
        name: "CacheModel",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Registers a named cache model with driver selection, TTL defaults, serialization hooks, and optional Redis-specific overrides.",
        exampleTitle: "Register a cache model",
        exampleCode: [
          'import { CacheModel } from "@xtaskjs/cache";',
          "",
          '@CacheModel({ name: "products", ttl: "5m", driver: "redis" })',
          "export class ProductCacheModel {}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-injectservice",
        name: "InjectCacheService",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects CacheService for model-level operations such as listModels(), get(), set(), remember(), delete(), and clear().",
        exampleTitle: "Inject the cache service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CacheService, InjectCacheService } from "@xtaskjs/cache";',
          "",
          "@Service()",
          "export class CacheInspectorService {",
          "  constructor(",
          "    @InjectCacheService()",
          "    private readonly cache: CacheService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-injectrepository",
        name: "InjectCacheRepository",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a model-scoped CacheRepository<T> so services can work directly with entries, hit metadata, and per-model TTL behavior.",
        exampleTitle: "Inject a cache repository",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CacheRepository, InjectCacheRepository } from "@xtaskjs/cache";',
          'import { ProductCacheModel } from "./product-cache.model";',
          "",
          "@Service()",
          "export class ProductCacheReader {",
          "  constructor(",
          "    @InjectCacheRepository(ProductCacheModel)",
          "    private readonly products: CacheRepository<any>",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-injectlifecycle",
        name: "InjectCacheLifecycleManager",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the lower-level cache lifecycle manager for diagnostics, initialization checks, repository resolution, and runtime state access.",
        exampleTitle: "Inject the cache lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectCacheLifecycleManager } from "@xtaskjs/cache";',
          "",
          "@Service()",
          "export class CacheLifecycleDiagnosticsService {",
          "  constructor(",
          "    @InjectCacheLifecycleManager()",
          "    private readonly lifecycle: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-injectadminservice",
        name: "InjectCacheAdminService",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects CacheAdminService for runtime inspection of models, entries, and effective HTTP cache metadata resolved from decorated routes.",
        exampleTitle: "Inject the cache admin service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CacheAdminService, InjectCacheAdminService } from "@xtaskjs/cache";',
          "",
          "@Service()",
          "export class CacheAdminInspector {",
          "  constructor(",
          "    @InjectCacheAdminService()",
          "    private readonly cacheAdmin: CacheAdminService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-cacheable",
        name: "Cacheable",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Reads through the configured repository. Cache hits skip method execution, while misses store the resolved result using an optional key builder, TTL override, and unless condition.",
        exampleTitle: "Read-through caching",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { Cacheable } from "@xtaskjs/cache";',
          'import { ProductCacheModel } from "./product-cache.model";',
          "",
          "@Service()",
          "export class ProductService {",
          '  @Cacheable({ model: ProductCacheModel, key: (id: string) => id, ttl: "15m" })',
          "  async getProduct(id: string) {",
          '    return { id, generatedAt: new Date().toISOString() };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-cacheput",
        name: "CachePut",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Always runs the method and then writes the returned value into the cache, optionally guarding the write with when and overriding the stored TTL.",
        exampleTitle: "Write-through cache refresh",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CachePut } from "@xtaskjs/cache";',
          'import { ProductCacheModel } from "./product-cache.model";',
          "",
          "@Service()",
          "export class ProductRefreshService {",
          '  @CachePut({ model: ProductCacheModel, key: (id: string) => id })',
          "  async refreshProduct(id: string) {",
          '    return { id, refreshedAt: new Date().toISOString() };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-cacheevict",
        name: "CacheEvict",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Removes one cached entry or clears an entire model before or after the wrapped method runs, with optional key resolution and conditional execution.",
        exampleTitle: "Evict one key or a whole model",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { CacheEvict } from "@xtaskjs/cache";',
          'import { ProductCacheModel } from "./product-cache.model";',
          "",
          "@Service()",
          "export class ProductInvalidationService {",
          '  @CacheEvict({ model: ProductCacheModel, key: (id: string) => id })',
          "  async evictProduct(id: string) {",
          "    return true;",
          "  }",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-cache-http",
    title: "HTTP And Browser Cache Policies",
    description:
      "HTTP response decorators from @xtaskjs/cache used to apply Cache-Control directives, validators, and Vary behavior to routes, plus injector access to HttpCacheService.",
    decorators: [
      {
        id: "decorator-cache-injecthttpservice",
        name: "InjectHttpCacheService",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects HttpCacheService so services or controllers can build cache headers manually, inspect policy normalization, or describe effective route behavior.",
        exampleTitle: "Inject the HTTP cache service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { HttpCacheService, InjectHttpCacheService } from "@xtaskjs/cache";',
          "",
          "@Service()",
          "export class CacheHeaderService {",
          "  constructor(",
          "    @InjectHttpCacheService()",
          "    private readonly httpCache: HttpCacheService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-cacheresponse",
        name: "CacheResponse",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Applies a full HTTP cache policy to responses, including visibility, max-age, stale directives, ETag, Last-Modified, Expires, Vary, and conditional application rules.",
        exampleTitle: "Apply a custom HTTP cache policy",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { CacheResponse } from "@xtaskjs/cache";',
          "",
          '@Controller("/reports")',
          "export class ReportsController {",
          '  @CacheResponse({ visibility: "private", maxAge: "60s", mustRevalidate: true })',
          '  @Get("/summary")',
          "  summary() {",
          '    return { ok: true };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-browsercache",
        name: "BrowserCache / CacheHeaders",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Alias names for CacheResponse() when the intent is browser-facing cache headers rather than a generic response policy decorator.",
        exampleTitle: "Alias for browser cache headers",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { BrowserCache } from "@xtaskjs/cache";',
          "",
          '@Controller("/articles")',
          "export class ArticleController {",
          '  @BrowserCache({ maxAge: "1m", staleWhileRevalidate: "30s" })',
          '  @Get("/:slug")',
          "  show() {",
          '    return { published: true };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-cacheview",
        name: "CacheView",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Applies the same HTTP cache policy surface as CacheResponse(), but only when the decorated route returns view(...).",
        exampleTitle: "Cache rendered views only",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { view } from "@xtaskjs/core";',
          'import { CacheView } from "@xtaskjs/cache";',
          "",
          '@Controller("/")',
          "export class WebController {",
          '  @CacheView({ maxAge: "10m", etag: true })',
          '  @Get("/")',
          "  home() {",
          '    return view("home", { title: "Welcome" });',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-nostore",
        name: "NoStore",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Disables browser storage for sensitive responses by combining no-store, no-cache, must-revalidate, and an already-expired Expires value.",
        exampleTitle: "Disable browser storage",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { NoStore } from "@xtaskjs/cache";',
          "",
          '@Controller("/drafts")',
          "export class DraftController {",
          '  @NoStore()',
          '  @Get("/preview")',
          "  preview() {",
          '    return { draft: true };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-nocache",
        name: "NoCache",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Forces revalidation by emitting no-cache and must-revalidate semantics while still allowing conditional requests when other validators are enabled.",
        exampleTitle: "Force revalidation",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { NoCache } from "@xtaskjs/cache";',
          "",
          '@Controller("/profile")',
          "export class ProfileController {",
          '  @NoCache()',
          '  @Get("/")',
          "  show() {",
          '    return { profile: true };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-cache-varyby",
        name: "VaryBy",
        packageName: "@xtaskjs/cache",
        packagePath: "packages/cache/src/http-cache.decorators.ts",
        kind: "Class and method decorator",
        targets: "class or method",
        summary: "Appends Vary header values so downstream caches distinguish responses by headers such as Accept-Language or Authorization.",
        exampleTitle: "Vary by request headers",
        exampleCode: [
          'import { Controller, Get } from "@xtaskjs/common";',
          'import { BrowserCache, VaryBy } from "@xtaskjs/cache";',
          "",
          '@Controller("/catalog")',
          "export class CatalogController {",
          '  @BrowserCache({ maxAge: "2m" })',
          '  @VaryBy("accept-language", "authorization")',
          '  @Get("/")',
          "  list() {",
          '    return { ok: true };',
          "  }",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-scheduler",
    title: "Scheduler Jobs And Lifecycle",
    description:
      "Scheduling decorators from @xtaskjs/scheduler used to declare cron, interval, and timeout jobs and to inject runtime scheduler services.",
    decorators: [
      {
        id: "decorator-scheduler-cron",
        name: "Cron",
        packageName: "@xtaskjs/scheduler",
        packagePath: "packages/scheduler/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a cron-based recurring job with optional groups, retries, timezone overrides, and boot execution behavior.",
        exampleTitle: "Cron job",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { Cron } from "@xtaskjs/scheduler";',
          "",
          "@Service()",
          "export class ReportsScheduler {",
          '  @Cron("0 */5 * * * *", { name: "reports.flush", group: ["reports", "nightly"] })',
          "  flushReports() {",
          '    console.log("flush pending reports");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-scheduler-every",
        name: "Every / Interval",
        packageName: "@xtaskjs/scheduler",
        packagePath: "packages/scheduler/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a fixed-interval recurring job. Interval is an alias of Every for projects that prefer the more explicit name.",
        exampleTitle: "Interval job",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { Every } from "@xtaskjs/scheduler";',
          "",
          "@Service()",
          "export class CacheScheduler {",
          '  @Every("10m", { name: "cache.compact", runOnInit: true })',
          "  compact() {",
          '    console.log("compact cache");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-scheduler-timeout",
        name: "Timeout",
        packageName: "@xtaskjs/scheduler",
        packagePath: "packages/scheduler/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a one-shot delayed task that runs after startup instead of on a recurring cadence.",
        exampleTitle: "Delayed warmup job",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { Timeout } from "@xtaskjs/scheduler";',
          "",
          "@Service()",
          "export class WarmupScheduler {",
          '  @Timeout("30s", { name: "cache.warmup" })',
          "  warmup() {",
          '    console.log("warm cache once after startup");',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-scheduler-injectservice",
        name: "InjectSchedulerService",
        packageName: "@xtaskjs/scheduler",
        packagePath: "packages/scheduler/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects SchedulerService so services or controllers can inspect jobs and trigger groups or individual jobs manually.",
        exampleTitle: "Inject scheduler service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSchedulerService, SchedulerService } from "@xtaskjs/scheduler";',
          "",
          "@Service()",
          "export class SchedulerInspector {",
          "  constructor(",
          "    @InjectSchedulerService()",
          "    private readonly scheduler: SchedulerService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-scheduler-injectlifecycle",
        name: "InjectSchedulerLifecycleManager",
        packageName: "@xtaskjs/scheduler",
        packagePath: "packages/scheduler/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the SchedulerLifecycleManager for lower-level control over startup state, active handles, and discovered job metadata.",
        exampleTitle: "Inject scheduler lifecycle",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSchedulerLifecycleManager } from "@xtaskjs/scheduler";',
          "",
          "@Service()",
          "export class SchedulerDiagnosticsService {",
          "  constructor(",
          "    @InjectSchedulerLifecycleManager()",
          "    private readonly lifecycle: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-socket-io",
    title: "Socket.IO Realtime Gateways",
    description:
      "Gateway, event, and injector decorators exported by @xtaskjs/socket-io. These keep realtime handlers inside regular xtaskjs services and wire Socket.IO namespaces into the DI container.",
    decorators: [
      {
        id: "decorator-socket-io-gateway",
        name: "SocketGateway",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Class decorator",
        targets: "class",
        summary: "Marks a DI-managed service as a realtime gateway, assigning namespace, optional name, groups, and disabled state for lifecycle discovery.",
        exampleTitle: "Decorated realtime gateway",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { SocketGateway } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          '@SocketGateway({ namespace: "/chat", group: ["realtime", "chat"] })',
          "export class ChatGateway {}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-connection",
        name: "OnSocketConnection",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Runs when a client connects to the gateway namespace and receives the connected socket plus handler context.",
        exampleTitle: "Track client connections",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { OnSocketConnection, SocketGateway } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          '@SocketGateway({ namespace: "/chat" })',
          "export class PresenceGateway {",
          "  @OnSocketConnection()",
          "  onConnect(socket: any, context: { namespace: any }) {",
          '    context.namespace.emit("presence.updated", { socketId: socket.id });',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-disconnect",
        name: "OnSocketDisconnect",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Runs when Socket.IO disconnects a client, making it a good place to update presence, release room state, or log disconnect reasons.",
        exampleTitle: "Handle disconnect reasons",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { OnSocketDisconnect, SocketGateway } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          '@SocketGateway({ namespace: "/chat" })',
          "export class PresenceGateway {",
          "  @OnSocketDisconnect()",
          "  onDisconnect(reason: string, socket: any) {",
          '    console.log(`socket ${socket.id} disconnected: ${reason}`);',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-event",
        name: "OnSocketEvent / SubscribeMessage",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a named Socket.IO event handler, optionally overrides namespace or handler options, and automatically acknowledges returned values when the client expects an ack.",
        exampleTitle: "Named chat event with acknowledgement",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { OnSocketEvent, SocketGateway } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          '@SocketGateway({ namespace: "/chat" })',
          "export class ChatGateway {",
          '  @OnSocketEvent("chat.message")',
          "  onMessage(payload: { text: string }, context: { socket: any }) {",
          '    return { ok: true, socketId: context.socket.id, text: payload.text };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-injectservice",
        name: "InjectSocketService",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects SocketIoService so controllers and services can emit events, inspect namespaces, and list discovered gateways.",
        exampleTitle: "Inject the socket helper service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSocketService, SocketIoService } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          "export class AnnouncementService {",
          "  constructor(",
          "    @InjectSocketService()",
          "    private readonly sockets: SocketIoService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-injectlifecycle",
        name: "InjectSocketLifecycleManager",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the SocketIoLifecycleManager for lower-level diagnostics, gateway inspection, namespace access, or manual emit control.",
        exampleTitle: "Inject the socket lifecycle manager",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSocketLifecycleManager } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          "export class SocketDiagnosticsService {",
          "  constructor(",
          "    @InjectSocketLifecycleManager()",
          "    private readonly lifecycle: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-injectserver",
        name: "InjectSocketServer",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects the root Socket.IO server instance when low-level adapter APIs or global broadcasts are required.",
        exampleTitle: "Inject the root Socket.IO server",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSocketServer } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          "export class SocketServerBridge {",
          "  constructor(",
          "    @InjectSocketServer()",
          "    private readonly server: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-socket-io-injectnamespace",
        name: "InjectSocketNamespace",
        packageName: "@xtaskjs/socket-io",
        packagePath: "packages/socket-io/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects one namespace instance by name so services can target a specific room topology without routing every emit through the root server.",
        exampleTitle: "Inject a named namespace",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectSocketNamespace } from "@xtaskjs/socket-io";',
          "",
          "@Service()",
          "export class ChatNamespacePublisher {",
          "  constructor(",
          '    @InjectSocketNamespace("/chat")',
          "    private readonly namespace: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
  {
    id: "decorators-queues",
    title: "Queues And Messaging",
    description:
      "Queue consumer, publish, and injector decorators exported by @xtaskjs/queues. These wire broker transports and in-memory queues into DI-managed services.",
    decorators: [
      {
        id: "decorator-queues-handler",
        name: "QueueHandler",
        packageName: "@xtaskjs/queues",
        packagePath: "packages/queues/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a named queue consumer for a concrete queue and supports retries, groups, dead-letter routing, and transport selection.",
        exampleTitle: "Queue-specific consumer",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { QueueHandler } from "@xtaskjs/queues";',
          "",
          "@Service()",
          "export class OrdersConsumer {",
          '  @QueueHandler("orders.created", {',
          '    name: "orders.created.consumer",',
          '    transportName: "memory",',
          '    maxRetries: 3,',
          '    deadLetterQueue: "orders.dead",',
          "  })",
          "  async onOrderCreated(payload: { orderId: string }) {",
          '    console.log("created", payload.orderId);',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-queues-pattern",
        name: "QueuePattern",
        packageName: "@xtaskjs/queues",
        packagePath: "packages/queues/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Registers a topic or pattern-based listener that can subscribe to wildcards or broker topic filters depending on the transport.",
        exampleTitle: "Pattern listener",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { QueuePattern } from "@xtaskjs/queues";',
          "",
          "@Service()",
          "export class AuditConsumer {",
          '  @QueuePattern("orders.*", {',
          '    name: "orders.audit",',
          '    transportName: "rabbitmq",',
          "  })",
          "  onOrderEvent(payload: any, context: { queue: string }) {",
          '    console.log(context.queue, payload);',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-queues-publishtoqueue",
        name: "PublishToQueue",
        packageName: "@xtaskjs/queues",
        packagePath: "packages/queues/src/decorators.ts",
        kind: "Method decorator",
        targets: "method",
        summary: "Publishes the resolved method result to a queue after the method completes while preserving the original return value.",
        exampleTitle: "Publish method result",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { PublishToQueue } from "@xtaskjs/queues";',
          "",
          "@Service()",
          "export class CheckoutService {",
          '  @PublishToQueue("checkout.completed", { transportName: "memory" })',
          "  complete(orderId: string) {",
          '    return { orderId, status: "completed" };',
          "  }",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-queues-injectservice",
        name: "InjectQueueService",
        packageName: "@xtaskjs/queues",
        packagePath: "packages/queues/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects QueueService so services or controllers can publish messages, create producers, and inspect runtime consumers and transports.",
        exampleTitle: "Inject queue service",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectQueueService, QueueService } from "@xtaskjs/queues";',
          "",
          "@Service()",
          "export class QueuePublisher {",
          "  constructor(",
          "    @InjectQueueService()",
          "    private readonly queues: QueueService",
          "  ) {}",
          "}",
        ].join("\n"),
      },
      {
        id: "decorator-queues-injecttransport",
        name: "InjectQueueTransport",
        packageName: "@xtaskjs/queues",
        packagePath: "packages/queues/src/decorators.ts",
        kind: "Parameter and property decorator",
        targets: "constructor parameter or property",
        summary: "Injects a named QueueTransport implementation when low-level broker publish or subscription control is required.",
        exampleTitle: "Inject named queue transport",
        exampleCode: [
          'import { Service } from "@xtaskjs/core";',
          'import { InjectQueueTransport } from "@xtaskjs/queues";',
          "",
          "@Service()",
          "export class QueueTransportDiagnostics {",
          "  constructor(",
          '    @InjectQueueTransport("rabbitmq")',
          "    private readonly transport: any",
          "  ) {}",
          "}",
        ].join("\n"),
      },
    ],
  },
];

const decoratorCount = decoratorGroups.reduce((total, group) => total + group.decorators.length, 0);

const decoratorPackages = Array.from(
  new Set(decoratorGroups.flatMap((group) => group.decorators.map((decorator) => decorator.packageName)))
).sort((left, right) => left.localeCompare(right));

@Service()
export class DocumentationService {
  @InjectInternationalizationService()
  private readonly intl!: InternationalizationService;

  getHubViewModel(): DocumentationHubViewModel {
    const locale = this.intl.getCurrentLocale();
    const baseViewModel = this.getBaseViewModel("/documentation");

    return {
      ...baseViewModel,
      packageCount: packageDocs.length,
      sampleCount: sampleDocs.length,
      decoratorCount,
      highlights: localizeHighlights(locale, [
        {
          title: "Monorepo shape",
          text: "The upstream xtask repository groups runtime packages and sample applications in one workspace so APIs, adapters, decorators, and integrations evolve together.",
        },
        {
          title: "Decorator-first design",
          text: "Controllers, lifecycle hooks, guards, security rules, persistence bindings, and mail delivery are expressed through decorators and resolved at runtime through the kernel and container.",
        },
        {
          title: "Adapter and integration portability",
          text: "Business logic stays stable while node-http, Express, Fastify, TypeORM, security, and mailer integrations change how the app is delivered and extended.",
        },
      ]),
      featuredPages: baseViewModel.docsPages.filter(
        (page) => page.href !== "/documentation"
      ),
      quickStart: {
        install: "npm install @xtaskjs/core @xtaskjs/common reflect-metadata",
        bootstrap: [
          'import "reflect-metadata";',
          'import { CreateApplication } from "@xtaskjs/core";',
          "",
          "await CreateApplication({",
          '  adapter: "node-http",',
          "  autoListen: true,",
          '  server: { host: "127.0.0.1", port: 3000 },',
          "});",
        ].join("\n"),
      },
    };
  }

  getArchitectureViewModel(): DocumentationArchitectureViewModel {
    const locale = this.intl.getCurrentLocale();

    return {
      ...this.getBaseViewModel("/documentation/architecture"),
      quickStart: {
        install: "npm install @xtaskjs/core @xtaskjs/common reflect-metadata",
        bootstrap: [
          'import "reflect-metadata";',
          'import { CreateApplication } from "@xtaskjs/core";',
          "",
          "await CreateApplication({",
          '  adapter: "node-http",',
          "  autoListen: true,",
          '  server: { host: "127.0.0.1", port: 3000 },',
          "});",
        ].join("\n"),
      },
      bootstrapFlow: localizeFlowSteps(locale, [
        {
          title: "1. Define components",
          text: "Decorate services, controllers, runners, and listeners in src/ so the container can discover them.",
        },
        {
          title: "2. Call CreateApplication()",
          text: "Core allocates the application lifecycle, kernel, and selected HTTP adapter.",
        },
        {
          title: "3. Boot the kernel",
          text: "The container scans project directories, registers providers, and resolves component metadata.",
        },
        {
          title: "4. Attach integrations",
          text: "Optional packages such as typeorm and security register lifecycle bindings into the same container.",
        },
        {
          title: "5. Register routes and events",
          text: "Controllers and listeners are translated into lifecycle routes, handlers, and execution pipelines.",
        },
        {
          title: "6. Listen and serve",
          text: "The selected adapter starts accepting requests and dispatches them back through the lifecycle.",
        },
      ]),
      requestFlow: localizeFlowSteps(locale, [
        {
          title: "Adapter receives request",
          text: "Express, Fastify, or node-http normalizes the request and forwards method + path into the framework.",
        },
        {
          title: "Route lookup",
          text: "ApplicationLifeCycle resolves the controller route registered during startup.",
        },
        {
          title: "Guards and auth",
          text: "Guards can block or enrich the route context before the handler executes.",
        },
        {
          title: "Pipes and middlewares",
          text: "Arguments are transformed and cross-cutting logic runs in a consistent order.",
        },
        {
          title: "Controller handler",
          text: "The handler returns JSON, a primitive response, or a view(...) result.",
        },
        {
          title: "Adapter response",
          text: "The adapter serializes the payload, renders a view, or sends the appropriate status code.",
        },
      ]),
      securityFlow: localizeFlowSteps(locale, [
        {
          title: "Strategy registration",
          text: "JWT or JWE strategies are registered before startup, defining token extraction and validation callbacks.",
        },
        {
          title: "Security initialization",
          text: "CreateApplication() initializes the security lifecycle and publishes auth services into the container.",
        },
        {
          title: "Guard activation",
          text: "Authenticated, Auth, Roles, and AllowAnonymous decorate routes and drive guard decisions.",
        },
        {
          title: "Context enrichment",
          text: "Successful authentication populates req.user, req.auth, response locals, and route execution context.",
        },
      ]),
    };
  }

  getPackagesViewModel(): DocumentationPackagesViewModel {
    const locale = this.intl.getCurrentLocale();

    return {
      ...this.getBaseViewModel("/documentation/packages"),
      packageCount: packageDocs.length,
      packages: localizePackageDocs(locale, packageDocs),
    };
  }

  getCliViewModel(): DocumentationCliViewModel {
    const locale = this.intl.getCurrentLocale();

    return {
      ...this.getBaseViewModel("/documentation/cli"),
      repoUrl: "https://github.com/xtaskjs/xtask-cli",
      packageName: "@xtaskjs/cli",
      binaryName: "xtask",
      commandCount: cliCommandDocs.length,
      optionGroupCount: cliOptionGroups.length,
      highlights: localizeHighlights(locale, cliHighlights),
      installDocs: localizeCliInstallDocs(locale, cliInstallDocs),
      commands: localizeCliCommandDocs(locale, cliCommandDocs),
      optionGroups: localizeCliOptionGroups(locale, cliOptionGroups),
      troubleshooting: localizeFlowSteps(locale, cliTroubleshootingFlow),
      notes: localizeStringList(locale, cliNotes),
    };
  }

  getPackageDetailViewModel(slug: string): DocumentationPackageDetailViewModel | undefined {
    const locale = this.intl.getCurrentLocale();
    const packages = localizePackageDocs(locale, packageDocs);
    const packageDoc = packages.find((entry) => entry.slug === slug);
    const packageDeepDive = packageDeepDiveDocs[slug];

    if (!packageDoc || !packageDeepDive) {
      return undefined;
    }

    const localizedDeepDive = localizePackageDeepDive(locale, packageDeepDive);

    return {
      ...this.getBaseViewModel(`/documentation/packages/${slug}`),
      packageDoc: {
        ...packageDoc,
        ...localizedDeepDive,
      },
      packageRepoUrl: `https://github.com/xtaskjs/xtask/tree/main/${packageDoc.path}`,
      sampleLinks: splitSampleCoverage(packageDoc.sample).map((sampleName) => ({
        name: sampleName,
        href: `/documentation/samples#sample-${sampleName}`,
      })),
      relatedPackages: packageDeepDive.related
        .map((relatedSlug) => packages.find((entry) => entry.slug === relatedSlug))
        .filter((entry): entry is PackageDoc => Boolean(entry))
        .map((entry) => ({
          name: entry.name,
          path: entry.path,
          tagline: entry.tagline,
          href: `/documentation/packages/${entry.slug}`,
        })),
      apiGroups: localizePackageApiGroups(locale, generatedPackageApiGroups[slug] || []),
    };
  }

  getSamplesViewModel(): DocumentationSamplesViewModel {
    const locale = this.intl.getCurrentLocale();

    return {
      ...this.getBaseViewModel("/documentation/samples"),
      sampleCount: sampleDocs.length,
      samples: localizeSampleDocs(locale, sampleDocs),
    };
  }

  getDecoratorsViewModel(): DocumentationDecoratorsViewModel {
    const locale = this.intl.getCurrentLocale();

    return {
      ...this.getBaseViewModel("/documentation/decorators"),
      decoratorCount,
      decoratorGroupCount: decoratorGroups.length,
      packageCoverage: decoratorPackages,
      decoratorGroups: localizeDecoratorGroups(locale, decoratorGroups),
    };
  }

  private getBaseViewModel(currentHref: string): DocumentationViewModel {
    return localizeDocumentationBase(this.intl.getCurrentLocale(), {
      title: "xTaskjs Documentation",
      repoUrl: "https://github.com/xtaskjs/xtask",
      docsPages: [
        {
          href: "/documentation",
          label: "Overview",
          description: "Hub with summary, quick start, and section entry points.",
          isCurrent: currentHref === "/documentation",
        },
        {
          href: "/documentation/architecture",
          label: "Architecture",
          description: "Boot lifecycle, request pipeline, and security flow diagrams.",
          isCurrent: currentHref === "/documentation/architecture",
        },
        {
          href: "/documentation/packages",
          label: "Packages",
          description: "Detailed reference for core, common, adapters, TypeORM, security, mailer, internationalization, scheduler, cache, queues, and value objects.",
          isCurrent:
            currentHref === "/documentation/packages" ||
            currentHref.startsWith("/documentation/packages/"),
        },
        {
          href: "/documentation/cli",
          label: "CLI",
          description: "Console client installation, command reference, and generation workflow.",
          isCurrent: currentHref === "/documentation/cli",
        },
        {
          href: "/documentation/decorators",
          label: "Decorators",
          description: "Decorator catalog organized by type and package, with examples.",
          isCurrent: currentHref === "/documentation/decorators",
        },
        {
          href: "/documentation/samples",
          label: "Samples",
          description: "Official sample applications and how to use them.",
          isCurrent: currentHref === "/documentation/samples",
        },
      ],
    });
  }
}

const splitSampleCoverage = (value: string): readonly string[] =>
  Array.from(value.matchAll(/\b\d{2}-[a-z0-9_]+\b/gi), (match) => match[0]);